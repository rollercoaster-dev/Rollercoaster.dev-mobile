# Copy to `.env` (gitignored, see .gitignore:18) and fill in.
# These are throwaway burner-account credentials — see README "Identity" for why.
# Never commit real values. The app password is NOT the account password:
# generate one at Settings -> Privacy and Security -> App Passwords.

PDS_URL=https://bsky.social
ATP_HANDLE=your-burner-handle.bsky.social
ATP_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
