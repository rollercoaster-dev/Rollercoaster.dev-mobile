import { StyleSheet } from "react-native-unistyles";

export const styles = StyleSheet.create(() => ({
  // Owns the space under the header and shrinks with the keyboard.
  frame: {
    flex: 1,
  },
}));
