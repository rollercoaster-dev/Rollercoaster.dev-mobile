# Development Plan: Issue #636

## Issue Summary

**Title**: [Test] Required E2E gate is 3/6 red on main — bake-recovery, full-ride, step-timing-editor
**Type**: bug (test infra + one production log-severity fix)
**Complexity**: MEDIUM
**Estimated Lines**: ~180–260 lines across production code, one flow file, tests, and the tracked report swap

## Environment (confirmed at implementation time)

iOS Simulator + Maestro are **available in this working environment**. Boot the app with:

```bash
bun run emulator:ios     # root package.json:31 → apps/native-rd/scripts/run-ios.sh
```

Consequently **all five steps, including the device-dependent ones (3, 4, 5), are executable here.** No step needs to be handed off to another machine. Any earlier note in this plan suggesting otherwise was written before the simulator was confirmed and is superseded by this section.

## Intent Verification

- [x] `bun run test:e2e:required` runs `bake-recovery.yaml` end to end and reaches `finish-reveal-stage` (phase 3), with no LogBox toast intercepting the `finish-celebrate-cta` tap.
- [x] `step-timing-editor.yaml`'s `edit-goal-step-timing-.*-depends-on-toggle` assertion passes because the flow scrolls it into view before asserting, without any change to `StepTimingEditor`/`DependencyPicker` production code (both already render the toggle unconditionally).
- [x] `full-ride.yaml` reaches `focus-current-task-title` = "Charlie step" after Alpha auto-advances, and the root cause (once confirmed on-device) is fixed at its source rather than papered over with a longer timeout.
- [x] `e2e/reports/` contains a new tracked run record (6/6 or the then-current required count) that replaces `full-ride-7e6149e.md`, generated from a real `bun run test:e2e:required` run, not hand-written.
- [x] A genuine future bake failure (not the deliberate no-evidence gate) still calls `logger.error` and `reportError` exactly as before — the fix narrows only the one expected, UI-recoverable rejection path.

## Dependencies

| Issue | Title                                                                                                                           | Status                              | Type      |
| ----- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | --------- |
| #635  | Badge can be minted for a goal with zero completed steps — bake-recovery depends on it                                          | 🔴 Open (referenced, not a blocker) | Related   |
| #598  | eddsa-rdfc-2022 proof + resolvable did:key — close gaps 5 and 7                                                                 | 🔴 Open (referenced, not a blocker) | Related   |
| #619  | test(native-rd): close out Set B & C — added `step-timing-editor.yaml`                                                          | ✅ Merged                           | Prior art |
| #502  | test(e2e): canonical Full Ride lifecycle — added `full-ride.yaml`/`bake-recovery.yaml`, and the existing `App.tsx` LogBox guard | ✅ Merged                           | Prior art |

**Status**: ✅ No hard dependencies. The issue body mentions #635 and #598 only as related context ("this flow also needs rewriting for a separate reason", "how #598's … gets verified") — neither is phrased as `Blocked by`/`Depends on`/`After`, and #636's own scope (make the existing three flows pass against current, shipped behavior) does not require either to land first.

## Objective

Get `bun run test:e2e:required` back to green by fixing the three flows the issue diagnoses, without touching the two that already pass (`settings-theme-persists-restart`, `settings-theme-switch`, `badge-view`), then regenerate the tracked report the gate's own doc convention (`e2e/reports/<flow>-<sha>.md`) requires as evidence.

## Decisions

| ID  | Decision                                                                                                                                                                                                                                                                | Alternatives Considered                                                                                                                                                                                        | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | Fix `bake-recovery` by giving the deliberate "no evidence" gate rejection its own `BadgeGateError` class in `useCreateBadge.ts` and routing it through `logger.info` instead of `logger.error`, rather than relying on the existing `LogBox.ignoreAllLogs(true)` guard. | (a) Trust the existing guard (`App.tsx:33-39`, added in #502) as sufficient. (b) Add an explicit Maestro "dismiss LogBox" step between phases. (c) String-match `err.message` in the catch instead of a class. | (a) is already in place, gated correctly on `EXPO_PUBLIC_E2E_MODE` (set by `ios:e2e`, `apps/native-rd/package.json:11`), and the tracked green run at `7e6149e` proves it _did_ work once with byte-identical `App.tsx` (`git diff 7e6149e HEAD -- App.tsx` is empty) — so it is not reliable enough to depend on alone; the underlying flakiness is unexplained and out of reach without device access. (b) is infeasible: LogBox is a native overlay Maestro's hierarchy dump cannot see (`App.tsx:33-34`'s own comment), so there is no stable `id:`/`text:` to target for a dismiss tap — the issue's own diagnosis shows tapping "Dismiss" _after_ the fact doesn't help because the mis-tap already happened. (c) is fragile against copy edits. `logger.info` calls `console.info` (`src/shims/rd-logger.js:36`), which LogBox does not intercept (only `console.error`/`console.warn` are patched), so this closes the hole deterministically regardless of any RN/Expo LogBox quirk.                                                                                                                                                                                                                                                                                                                                                                                        |
| D2  | Keep `logger.error` + `reportError` for every _other_ `useCreateBadge` failure path (PNG read/write, sign, `createBadge`/`updateBadge`, `completeGoal`) unchanged.                                                                                                      | Downgrade the whole outer catch to `warn`/`info`.                                                                                                                                                              | Those are genuine unexpected faults a maintainer needs to see in Sentry/dev LogBox; only the evidence-gate rejection is "expected and already has its own recovery UI" (`finish-baking-error-alert` + Retry). Narrowing the fix to one `instanceof` branch keeps the blast radius to the one path the issue diagnoses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D3  | Fix `step-timing-editor.yaml` by adding a `scrollUntilVisible` for `edit-goal-step-timing-.*-depends-on-toggle` before the existing `assertVisible`/`tapOn` pair, with **no production code change**.                                                                   | (a) Make `DependencyPicker`'s candidate list collapse the toggle when `candidates` is empty (the issue's own hypothesis). (b) Change the toggle's testID.                                                      | Both (a) and (b) are ruled out by reading the code: `StepTimingEditor.picker.tsx:45-49` renders the `Pressable` (and therefore `testID={`${testID}-toggle`}`) unconditionally — the `candidates.length === 0` branch (`StepTimingEditor.picker.tsx:103-110`) only swaps the *candidate list* body for empty-state copy, never touches the toggle. `StepTimingEditor.tsx:290` builds the outer testID as `${testID}-depends-on`, which composes to exactly `edit-goal-step-timing-<id>-depends-on-toggle`— matching the flow's regex verbatim. The real cause:`StepDayGrid`(a full month calendar,`StepDayGrid.tsx`) renders between the editor's root and `DependencyPicker` (`StepTimingEditor.tsx:268-291`), and the flow centers the *collapsed* row on screen before tapping it open (`step-timing-editor.yaml:126-134`) — so the expanded content's `-depends-on-toggle`lands below the fold. This matches the issue's own clue exactly ("the`-editor` assertion immediately before it passes"): the editor's root mounts where the centered row was, but the toggle is pushed off-screen by the grid above it. The fix mirrors the file's own established pattern for exactly this situation (`step-timing-editor.yaml:126-134`, `:149-154`already`scrollUntilVisible` before addressing a just-mounted/off-screen element).                                                   |
| D4  | Treat `full-ride`'s "Charlie step never becomes focused" failure as **requiring an on-device diagnostic step before a durable fix**, rather than shipping a speculative change to the reorder/reparent/resolver code.                                                   | Ship a "best guess" fix now (e.g., widen a timeout, or touch `resolveNextActionableStep`/`applyStepOrdinals`).                                                                                                 | Exhaustive static review found no bug: `resolveNextActionableStep` (`src/db/queries.ts:630-687`), `groupStepsByParent`/`flattenGroupedSteps` (`src/db/queries.ts:458-489`), and the reorder/reparent ordinal math (`applyStepOrdinals` `src/db/queries.ts:1064-1099`, `handleReparentStep`'s `nextOrdinal` calc `EditModeScreen.tsx:480-497`) all compute the expected root order `[Alpha, Charlie, Bravo]` after the flow's rename → reorder → demote → promote sequence (comments 13–18 in `full-ride.yaml`), and this exact ordinal math is independently pinned by four passing unit tests (`EditModeScreen.test.tsx:1276-1361`, the `"reparent wiring (#330)"` block, including a promote-back case whose expected ordinal (`2`) matches this scenario). Shipping a change to code that is already correct-by-test would be guessing. The stronger, code-grounded lead is D1's own family of bug: `EditModeScreen.tsx` has eight other `console.error` call sites in mutation-failure catch blocks (`:166,191,256,319,368,396,447,467,511,534`) that would raise the identical LogBox-eats-a-tap hazard if any Evolu write in the 15-command rename/reorder/reparent/complete sequence actually failed on-device (jest mocks Evolu entirely, so a real-device-only failure mode wouldn't show up in unit tests). This is a plausible, testable hypothesis, not a confirmed one. |
| D5  | Regenerate `e2e/reports/full-ride-7e6149e.md`'s replacement by actually running `bun run test:e2e:required` on a machine with the iOS Simulator + Maestro, not by hand-authoring the report.                                                                            | Write the report from the fix's expected outcome.                                                                                                                                                              | The existing report is explicitly "the artifact acceptance criterion... records a real simulator run... not a cached turbo replay" (`e2e/reports/full-ride-7e6149e.md:3-4`); a hand-authored replacement would violate the exact convention this issue is trying to restore trust in. **This environment DOES have an iOS Simulator** — boot it with `bun run emulator:ios` (root `package.json:31` → `apps/native-rd/scripts/run-ios.sh`). Every device-dependent step in this plan is therefore executable here; the research-time "no simulator" caveat is void.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Affected Areas

- `apps/native-rd/src/hooks/useCreateBadge.ts`: add `BadgeGateError`, branch the outer catch on it (D1, D2).
- `apps/native-rd/src/hooks/__tests__/useCreateBadge.test.ts`: assert the no-evidence path no longer calls `logger.error`/`reportError`, and that a genuine failure still does.
- `apps/native-rd/e2e/flows/step-timing-editor.yaml`: add one `scrollUntilVisible` before the depends-on toggle assertion (D3).
- `apps/native-rd/e2e/flows/full-ride.yaml`: instrumented temporarily for diagnosis (screenshots), then whatever durable change the diagnosis calls for — production code (most likely `EditModeScreen.tsx` or `useCreateBadge.ts`-adjacent) or the flow itself, TBD by D4's investigation.
- `apps/native-rd/e2e/reports/full-ride-7e6149e.md`: deleted.
- `apps/native-rd/e2e/reports/full-ride-<new-sha>.md`: added, from a real run (D5).

## Implementation Plan

### Step 1: Stop routing the expected bake-recovery gate rejection through `console.error`

**Files**: `apps/native-rd/src/hooks/useCreateBadge.ts`, `apps/native-rd/src/hooks/__tests__/useCreateBadge.test.ts`
**Commit**: `fix(native-rd): keep the expected no-evidence bake rejection off LogBox`
**Changes**:

- [ ] Add `class BadgeGateError extends Error {}` near the top of `useCreateBadge.ts` (after imports).
- [ ] Change the `canCompleteGoal` rejection (currently `throw new Error("Cannot complete goal: no evidence attached. Add at least one evidence item first.")`, `useCreateBadge.ts:364-366`) to `throw new BadgeGateError(...)` with the same message (preserves `result.current.error` for existing tests).
- [ ] In the outer catch (`useCreateBadge.ts:410-421`), branch: `if (err instanceof BadgeGateError)` → `logger.info("Badge bake blocked: evidence gate not satisfied", { goalId })` and skip `reportError`; else keep the existing `logger.error(...)` + `reportError(...)` exactly as today.
- [ ] Add a unit test asserting the no-evidence path does **not** call the mocked `reportError`, alongside the existing `"still throws when neither scope has a typed row"` test (which already exercises this exact branch and keeps passing unchanged on its `result.current.error`/`status` assertions).
- [ ] Add/confirm a unit test that a genuine failure (e.g. `mockCreateBadge` returning `{ ok: false }`) still calls `reportError` — regression guard for D2.

### Step 2: Scroll the `depends-on` toggle into view before asserting it

**Files**: `apps/native-rd/e2e/flows/step-timing-editor.yaml`
**Commit**: `fix(e2e): scroll to the depends-on toggle before asserting it in step-timing-editor`
**Changes**:

- [ ] Between the existing `assertVisible: edit-goal-step-timing-.*-editor` and the `assertVisible`/`tapOn` pair on `edit-goal-step-timing-.*-depends-on-toggle` (`step-timing-editor.yaml:138-143`), insert a `scrollUntilVisible` targeting the toggle regex, `direction: DOWN`, `centerElement: true` — the same shape already used at `:126-131` and `:149-154` in this file.
- [ ] Extend the file's header comment (or an inline comment at the new step) to record why: `StepDayGrid`'s month calendar sits between the editor root and the toggle, and the flow only centered the _collapsed_ row before opening it.

### Step 3: Diagnose the full-ride Charlie-step handoff on-device

**Files**: `apps/native-rd/e2e/flows/full-ride.yaml` (temporary instrumentation, not committed as-is)
**Commit**: none yet — diagnostic only, mirroring the "instrumented copy of the flow" methodology the issue itself used for bake-recovery
**Changes**:

- [ ] Run `full-ride.yaml` with `takeScreenshot` calls bracketing comments 15–23 (reorder → demote → promote-back → capture → mark-complete) and check `maestro hierarchy` for any LogBox artifact at the moment of failure.
- [ ] If a LogBox toast/redbox is present: identify which of the eight `console.error` sites in `EditModeScreen.tsx` (`:166,191,256,319,368,396,447,467,511,534`) fired, and fix that mutation's actual failure (not just its logging) — a `console.error` firing here means a write genuinely failed, which is a real bug distinct from D1's "expected rejection" case (nothing here is an expected/by-design failure).
- [ ] If no LogBox artifact is present: capture the actual rendered `focus-current-task-title` value at the failure point (Alpha? Bravo? blank?) and use that to determine whether it's an Evolu-reactivity race (e.g., the title-debounce commit noted in `full-ride.yaml:160-175` racing the subsequent `Done` tap) rather than an ordering bug.

### Step 4: Apply the full-ride fix found in Step 3

**Files**: TBD by Step 3's finding (most likely `EditModeScreen.tsx` or `useCreateBadge.ts`-adjacent code; possibly `full-ride.yaml` itself if it's a flow-authoring gap)
**Commit**: `fix(native-rd): <specific root cause found in step 3>`
**Changes**:

- [ ] Fix the confirmed root cause.
- [ ] Remove any temporary screenshot instrumentation added in Step 3 from the committed flow.
- [ ] If Step 3 turns up something big enough to threaten the ~500-line PR budget, split it into its own follow-up issue and land Steps 1/2/5 first — see Not in Scope.

### Step 5: Refresh the tracked E2E run record

**Files**: `apps/native-rd/e2e/reports/full-ride-7e6149e.md` (deleted), `apps/native-rd/e2e/reports/full-ride-<new-sha>.md` (added)
**Commit**: `test(native-rd): refresh the tracked E2E run record — <n>/6 green`
**Changes**:

- [ ] Boot the simulator with `bun run emulator:ios` (available in this environment), then run `bun run test:e2e:required` (and `evidence-viewer.yaml` separately, per the README's existing convention).
- [ ] Author the new report in the same shape as `full-ride-7e6149e.md` (Result table, Environment table, What the ride proved, any defects surfaced), naming it after the commit SHA the green run was taken at.
- [ ] Delete `full-ride-7e6149e.md`.

## Testing Strategy

- [ ] Unit tests for `useCreateBadge`'s gate-vs-genuine-failure branching (Jest 30) — extend `apps/native-rd/src/hooks/__tests__/useCreateBadge.test.ts`, mirroring its existing `mockReportError`-adjacent assertions.
- [ ] No new production code in Step 2 — verified by running `bun run test:e2e:single e2e/flows/step-timing-editor.yaml` against the local simulator (`bun run emulator:ios` first).
- [ ] Manual: `bun run test:e2e:required` full pass, once Steps 1, 2, and 4 land, before authoring Step 5's report.

## Not in Scope

| Item                                                                                                           | Reason                                                                                                                                                                                                                                                                   | Follow-up                                                    |
| -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ |
| Rewriting `bake-recovery.yaml`'s overall approach                                                              | Tracked separately; the issue itself flags this ("this flow also needs rewriting for a separate reason — see #635")                                                                                                                                                      | #635                                                         |
| `evidence-viewer.yaml` (optional-tagged flow)                                                                  | Not part of the required gate; the issue's "3/6 red" and "Done when" both scope to the six flows under `test:e2e:required`'s current pass/fail, not the optional flow                                                                                                    | none                                                         |
| Investigating _why_ `LogBox.ignoreAllLogs(true)` doesn't reliably suppress the overlay on this RN/Expo version | D1's fix sidesteps the question entirely (avoids `console.error`/`console.warn` for the one path this issue needs fixed) rather than resolving the underlying LogBox behavior, which needs device-level RN/Expo internals investigation out of scope for an E2E-gate fix | Worth a follow-up issue if LogBox-eaten-taps recur elsewhere |
| A large, unrelated fix if Step 3's diagnosis surfaces one                                                      | Keeps this PR mergeable independently per the ~500-line budget                                                                                                                                                                                                           | New issue, filed once Step 3 completes                       |

## Discovery Log

<!-- Entries added by implement skill:
- [YYYY-MM-DD HH:MM] <discovery description>
-->

- [2026-09-01] Confirmed iOS Simulator is available in this environment (`bun run emulator:ios`, root `package.json:31`). Supersedes D5's research-time assumption that device steps needed another machine — Steps 3, 4 and 5 are all in scope for this session.
- [2026-09-01] Started, then backed out of, Step 1: only reads happened, **no source file was modified**. `useCreateBadge.ts` imports end at `:63` (`Logger` from `../shims/rd-logger`), `const logger = new Logger("useCreateBadge")` at `:65` — `BadgeGateError` belongs immediately after that line. The gate `throw` is at `:363-366` and the outer catch at `:407-421` (both confirmed by reading, matching the plan's line refs).
- [2026-09-01] Step 1 landed as `3140f8c`: `BadgeGateError` + `logger.info` branch, plus two unit tests (gate path logs no error / reports nothing; a genuine `{ ok: false }` persistence failure still does both). `bake-recovery.yaml` then passed on the simulator, reaching `finish-reveal-stage`.
- [2026-09-01] Step 2 landed as `45322d9`, verified green by `maestro test e2e/flows/step-timing-editor.yaml`. D3's static diagnosis held exactly: no production change needed.
- [2026-09-01] Step 3 diagnosis — **D4's LogBox hypothesis was wrong; no LogBox artifact at the failure point.** The failure screenshot shows Focus on "Bravo step", and a `maestro hierarchy` dump taken at the failure point still shows `timeline-node-2-a` = Bravo, i.e. **the step-18 promote-back never committed**. Cause: with Bravo nested, `edit-goal-substep-un-nest-*` renders at y 788-824 while the `edit-goal-content` scroll viewport ends at y 808, so most of the row is clipped. iOS reports a frame for it anyway, so the bare `tapOn` aimed at its centre (~y 806), nothing received the tap, and Maestro reported COMPLETED. It was **not** the pinned `edit-goal-done-button` footer (y 710-758) absorbing the tap: that would have closed edit mode and failed step 18's very next command rather than step 23. Nor is it a production reachability defect — the list scrolls and the control is reachable once scrolled, which is what the fixed flow does. Bravo stayed Charlie's only child, so `resolveNextActionableStep` drilled into it once Alpha completed. Same shape as D3, twenty commands from the symptom.
- [2026-09-01] Step 4 landed as `62f113d`: `scrollUntilVisible` before the un-nest tap plus an `assertNotVisible` on the control immediately after, so a future silent no-op fails at the promote. Confirmed by replaying just the promote on the failed run's state — the timeline became `[Alpha, Charlie, Bravo]`. **No production change**: the reorder/reparent/resolver ordinal math was correct, as D4 suspected.
- [2026-09-01] Step 5: `bun run test:e2e:required` → **6/6 in 4m 37s, exit 0** at `62f113d`; `evidence-viewer.yaml` (optional) passes separately. Recorded at `e2e/reports/full-ride-62f113d.md`; `full-ride-7e6149e.md` deleted. Full Jest suite 215 suites / 10315 tests green.
- [2026-09-01] Environment note for the next run: a Metro from a sibling worktree (`misty-side`) owned port 8081, so the dev client was serving a different checkout. Had to stop it before `bun run ios:e2e`. This is the hazard the tracked report already documents under "exactly one Metro".
- [2026-09-01] `/code-review` since `4d3209a` (Standards + Spec axes) surfaced six findings, all applied in a follow-up commit: extracted the ad-hoc Logger-instance capture into a shared `src/__tests__/logger-helpers.ts` (`capturedLoggerFor`) and migrated `SettingsScreen.test.tsx`'s near-duplicate onto it; merged the double `require("../../services/sentry-report")`; dropped `export` from `BadgeGateError` (nothing outside the module uses it); deleted the now-dead `assertVisible` that sat directly after the identical `scrollUntilVisible` in `step-timing-editor.yaml`; added the `EXPO_PUBLIC_E2E_MODE` row the run-record convention requires (`e2e/README.md:188-194`) and switched the report to JUnit-exact per-flow times; inlined the Risk 1/Risk 2 evidence that had been cross-referenced to the deleted `full-ride-7e6149e.md`, and noted the supersession in the #502 plan. The Spec axis also caught the step-18 mechanism being overstated as "the tap hit the Done footer" — corrected everywhere to what the measured bounds actually support.
