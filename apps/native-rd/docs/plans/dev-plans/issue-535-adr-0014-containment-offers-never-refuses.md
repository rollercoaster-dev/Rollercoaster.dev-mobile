# Development Plan: Issue #535

## Issue Summary

**Title**: [ADR] ADR-0014: containment is informative in both directions — the app offers, never asserts, never refuses
**Type**: documentation (architecture decision record — no production code changes)
**Complexity**: SMALL (single new doc + index/header bookkeeping; sized by care of wording, not LOC)
**Estimated Lines**: ~140-180 (new ADR file) + ~10-15 (index.md Ledger row + Current-positions edit) + ~1-2 (ADR-0012/0013 status-line pointer edits) = **~155-200 lines total**

## Intent Verification

- [x] `apps/native-rd/docs/decisions/ADR-0014-containment-offers-never-refuses.md` exists, follows the house header block (`**Date:**` / `**Status:**` / `**Owner:**` / `**Supersedes:**`, `---`, then `## Context` → `## Decision` → `## Consequences` → `## Supersession`, closing italic acceptance line) per every sibling ADR (`ADR-0012-no-auto-judgment.md:1-11`, `ADR-0013-phase-b-consolidated-position.md:1-13`).
- [x] The unified rule appears as one sentence: "Evidence gates completion; structure only decides what is offered. Containment is informative in both directions: the app offers, never asserts, never refuses." — matching the issue body and `step-containment-semantics.md:466`.
- [x] The four-direction table (cascade-up / cascade-down / all set aside / child context on a leaf) is present, matching `step-containment-semantics.md:471-476`.
- [x] The three mechanical reasons a gate is out are recorded as the argument (tree-blind `completeStep` signature, CRDT non-invariance, three ungated sibling status writers) — not ADR exegesis.
- [x] ADR-0012's reopened Q9 is explicitly settled in the "offer, never automatic" direction, in **both** directions of the tree, with the reason for declining the permitted cascade-up stated (parent's own work is not entailed by "every child completed").
- [x] ADR-0010 is named as untouched, with the reason (nothing recommended refuses an action, so its C-order scope is never adjudicated).
- [x] The line "a `completed` parent means 'I'm done with my part of this,' not 'this subtree is closed'" appears **verbatim**.
- [x] `docs/decisions/index.md` gains an ADR-0014 Ledger row, and its **Current positions** table is updated in the same PR (not just the Ledger) — required by `index.md:1-8` ("Every PR that adds, accepts, or supersedes an ADR must update both the Current positions section and the Ledger below in the same PR").
- [x] `bun run format:check` passes on the changed `.md` files (the only docs CI gate — `.github/workflows/ci-docs.yml:1-40`; no markdownlint config exists in this repo).

## Dependencies

| Issue | Title                                        | Status                          | Type                                                          |
| ----- | -------------------------------------------- | ------------------------------- | ------------------------------------------------------------- |
| #533  | [Research] Parent ↔ sub-step semantics       | ✅ Merged (PR #539, 2026-07-31) | Hard input — the research doc this ADR records is landed      |
| #536  | [Fix] Resolver predicate split (F2)          | 🔴 Open                         | Downstream — sequenced **after** #535, not a dependency of it |
| #537  | [Fix] Focus Mode wires resolver result (F3)  | 🔴 Open                         | Downstream — sequenced **after** #535, not a dependency of it |
| #538  | [Feature] Parent-completion parts offer (F6) | 🔴 Open                         | Downstream — sequenced **after** #535, not a dependency of it |

**Status**: ✅ All dependencies met. The issue's own body states "Sequenced first" and "**Blocks:** #536 · #537 · #538" — #535 blocks those three, it is not blocked by anything. Labels confirm this (`dep:independent`). `has_blockers = false`.

## Objective

Write `docs/decisions/ADR-0014-containment-offers-never-refuses.md`, recording the containment rule the research already established (`step-containment-semantics.md`) as an accepted architecture decision: evidence gates completion, structure only offers, in both cascade directions — settling ADR-0012's reopened Q9 in the offer-only direction and leaving ADR-0010 untouched. Update `index.md`'s Ledger and Current-positions table in the same change, per the house mutability rule.

## Decisions

All code-answerable; resolved against existing ADRs and `index.md`, not invented.

| ID  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Alternatives Considered                                                           | Rationale (file:line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Filename: `ADR-0014-containment-offers-never-refuses.md`.                                                                                                                                                                                                                                                                                                                                                                                                           | `ADR-0014-parent-substep-containment.md`; `ADR-0014-no-completion-gate.md`        | Every existing filename names the _decision's core_, not the topic area — `ADR-0012-no-auto-judgment.md` (the guardrail's name), `ADR-0011-step-model-names.md` (what changed), `ADR-0010-phase-b-step-model-crosswalk.md` (what it produces). "Offers, never asserts, never refuses" is the decision's own one-line summary in both the issue title and `step-containment-semantics.md:466`.                                                                                                                                                                                                                                |
| D2  | ADR body order: `## Context` → `## Decision` → `## Consequences` → `## Supersession`, closing with an italic acceptance line (`_Accepted YYYY-MM-DD._`). No extra top-level sections needed (ADR-0010 added `## Structural Guardrails`, ADR-0012 added `## What does not change` — both because those ADRs carried forward guardrails from a superseded ADR; ADR-0014 doesn't need that pattern since it settles a question rather than reframing one).             | Add a `## What does not change` section naming ADR-0010's untouched status        | `ADR-0012-no-auto-judgment.md` header order (`:1-16`) and `ADR-0013-phase-b-consolidated-position.md` header order (`:1-15`) are the two most structurally similar precedents (both settle/restate a guardrail rather than introduce new scope).                                                                                                                                                                                                                                                                                                                                                                             |
| D3  | Owner: Joe. Date: **2026-08-04** (today; matches the convention that every ADR's `**Date:**` is the drafting/session date, e.g. `ADR-0013:3` is dated the day of its drafting session, separate from its later acceptance date in Status).                                                                                                                                                                                                                          | Backdate to 2026-07-30 (the research doc's date)                                  | `step-containment-semantics.md:3` is dated 2026-07-30 (the research session); ADR-0014 is a separate, later drafting act per the issue, so it gets its own date, matching how ADR-0012 (`2026-06-14`) postdates ADR-0010 (`2026-06-08`) despite discussing the same guardrail.                                                                                                                                                                                                                                                                                                                                               |
| D4  | Status line: **`Proposed — pending sign-off (hitl)`** at draft time, with a note that PR review/merge flips it to `Accepted <merge-date>`.                                                                                                                                                                                                                                                                                                                          | Mark `Accepted` immediately on draft                                              | The issue carries the `hitl` label (human-in-the-loop required) and every existing ADR's Status line names a real acceptance event (`ADR-0013:4-5` — "Accepted 2026-07-26 (Joe's sign-off on PR #518)"; `ADR-0012:4` — "Accepted 2026-07-26 — via acceptance of ADR-0013"). No existing ADR was ever _drafted_ with an unconditional "Accepted" before actual sign-off; `hitl` signals that sign-off hasn't happened yet. The implement/finalize skill should update this line to `Accepted <date> (Joe's sign-off on PR #<N>)` once merged, matching the `ADR-0013` phrasing exactly.                                       |
| D5  | `**Supersedes:**` line: **ADR-0013** — "the Q9 cascade-up permission only, restated unchanged from ADR-0012; settled here in the offer-only direction." ADR-0012 and ADR-0013 stay unedited historical records.                                                                                                                                                                                                                                                     | Supersede ADR-0012 directly (skip ADR-0013); supersede neither and just cite them | `ADR-0013:1-15` ("Reading rule") states explicitly: "This ADR and index.md are the current reference... Changes to anything restated here require a new ADR superseding ADR-0013." ADR-0013 restates ADR-0012's Q9-reopening "unchanged" (`ADR-0013` header note) and is the document `index.md`'s Current-positions table points readers to for Phase B — so per ADR-0013's own instruction, any settlement of something it restates must supersede _it_, not reach past it to ADR-0012. This is the same narrow-scope pattern ADR-0012 itself used against ADR-0010 ("the no-auto-state guardrail's name and scope only"). |
| D6  | `index.md` Ledger: add an ADR-0014 row (`Accepted` — or `Proposed`, matching D4 — with today's date), **and** edit ADR-0013's existing Ledger row to add a forward pointer: "Accepted — Q9's reopened cascade-up permission settled by [ADR-0014](./ADR-0014-containment-offers-never-refuses.md)" (append to the existing "Accepted" cell, do not remove the "Accepted 2026-07-26" fact).                                                                          | Leave ADR-0013's Ledger row untouched since its body isn't edited                 | `index.md`'s own rule (`:1-8`): "ADR headers (the Status line and supersession pointers) are the one mutable surface... Every PR that adds, accepts, or supersedes an ADR must update both the Current positions section and the Ledger... in the same PR." Precedent for a multi-clause status cell already exists: `ADR-0001`'s Ledger row reads "Accepted — Iteration B scope partially superseded by ADR-0006; consolidated in ADR-0013" (`index.md` Ledger table) — the exact "partially superseded, note the scope" shape D6 reuses.                                                                                   |
| D7  | `index.md` Current-positions table: the **Phase B** row's "Current reference" cell becomes "ADR-0013, ADR-0014"; its "Current position" prose gets a trailing clause noting containment cascade is settled offer-only (Q9). Do not give containment its own new topic row — it is a Phase B sub-decision, same as ADR-0011/ADR-0012 were folded into the existing Phase B row rather than getting their own rows.                                                   | New "Containment semantics" row in Current positions                              | The existing Phase B row already absorbs ADR-0010, 0011, 0012 as one topic with one reference cell pointing at the consolidating ADR-0013 (`index.md` Current-positions table, Phase B row); ADR-0014 is the same kind of narrow addendum, not a new topic axis. The "Reading rule" (`index.md:9-11`, "Never reconstruct current scope by diffing a supersession chain") requires this cell to be current, since it is the thing readers consult first.                                                                                                                                                                      |
| D8  | Commit type/scope: `docs(native-rd): ADR-0014 — containment offers, never refuses (#535)`. Single commit (doc + index.md edits are one atomic change; splitting them would leave index.md briefly pointing at a non-existent ADR).                                                                                                                                                                                                                                  | Two commits (ADR file, then index update)                                         | Matches the merged sibling PR's commit shape: `docs(native-rd): parent ↔ sub-step containment semantics research (#533) (#539)` (root `CLAUDE.md` recent-commits list) — one commit per doc landing, index update folded in.                                                                                                                                                                                                                                                                                                                                                                                                 |
| D9  | Queries.ts citations in the ADR are re-verified, not copied from the issue body: `completeStep` signature `queries.ts:854-858`; `status:` writes at `completeStep` `:882`, `uncompleteStep` `:901`, `pauseStep` `:924`, `resumeStep` `:943`; orphan-promotion-as-depth-cap at `resolveNextActionableStep` `:591-608`; `updateStep` (`:751-766`) confirmed to never write `status`. All matched the issue's own citations exactly on re-check — no drift to correct. | Trust the issue's line numbers unchecked                                          | Direct `sed`/`grep` verification against the current `queries.ts` in this session (2026-08-04) — every cited line/range matched. Recorded here so the implement step doesn't re-verify unnecessarily, and so a future reader knows these were checked, not assumed.                                                                                                                                                                                                                                                                                                                                                          |

## Affected Areas

- `apps/native-rd/docs/decisions/ADR-0014-containment-offers-never-refuses.md`: new file — the ADR itself.
- `apps/native-rd/docs/decisions/index.md`: add ADR-0014 Ledger row; edit ADR-0013's Ledger status cell (forward pointer); edit the Phase B row in Current positions (reference cell + prose clause).

No files under `apps/native-rd/src/` are touched. No `ADR-0010`, `ADR-0011`, or `ADR-0012` body text is edited — only status-line/Ledger pointers on `ADR-0013`, per the immutability rule.

## Implementation Plan

### Step 1: Draft ADR-0014

**Files**: `apps/native-rd/docs/decisions/ADR-0014-containment-offers-never-refuses.md`
**Commit**: `docs(native-rd): ADR-0014 — containment offers, never refuses (#535)`
**Changes**:

- [x] Header block: Date `2026-08-04`; Status `Proposed — pending sign-off (hitl)` (D4); Owner `Joe`; Supersedes line per D5.
- [x] `## Context`: the research (`step-containment-semantics.md`) found the app already implements one coherent rule at both the step and goal container levels — evidence gates completion, structure never gates it — but never wrote the rule down; name that ADR-0012's Q9 reopening ("all parts checked → parent done" is permissible bookkeeping) was left unsettled by ADR-0013's restatement and is the live question this ADR closes.
- [x] `## Decision`: state the one-sentence rule verbatim (Intent Verification item 2); include the four-direction table verbatim (item 3); state the three mechanical reasons a gate is out, each as its own short paragraph, citing `queries.ts:854-858` (tree-blind signature), the Evolu-CRDT non-invariance argument (two-device concurrent-write scenario), and the three-ungated-siblings table (`completeStep :882` evidence-gated only, `uncompleteStep :901` / `pauseStep :924` / `resumeStep :943` ungated; `updateStep :751-766` does not write status) — per D9's re-verified lines.
- [x] `## Decision` (continued): explicitly settle ADR-0012's reopened Q9 — name it as a **declined permission, not a reading of a prohibition** — state the reason ("every child completed" does not entail "the parent's own work is done") — and settle it in **both** directions of the tree (cascade-up: the Q9 invite; cascade-down: F6's parts offer), never automatic in either.
- [x] `## Decision` (continued): name ADR-0010 as explicitly untouched, with the reason (nothing recommended refuses an action; ADR-0010's C-order scope vs. A-containment scope is never adjudicated because it doesn't need to be).
- [x] `## Decision` (continued): include the verbatim line "a `completed` parent means 'I'm done with my part of this,' not 'this subtree is closed.'"
- [x] `## Decision` (continued): one line noting ADR-0012's hand-editable override (`:57`) is why delete stays out of the parts offer (#538) — delete is the one transition the override cannot reach.
- [x] `## Decision` (continued): one line noting ADR-0013's open depth question is not forced — depth is already one level in code, via orphan promotion (`queries.ts:591-608`).
- [x] `## Consequences`: this ADR is what #536/#537/#538 implement; they should land after it, not before (per the issue's own "Blocks" line). No schema, UI, or state-machine detail is decided here — that's #536-#538's job.
- [x] `## Supersession`: per D5 — supersedes ADR-0013 for the Q9 cascade-up permission scope only; ADR-0012 and ADR-0013 remain unedited historical records; future changes to this containment rule require a new ADR superseding ADR-0014, not an edit in place.
- [x] Closing italic line: `_Drafted 2026-08-04, pending sign-off._` (swap to `_Accepted <date>._` once merged, per D4).

### Step 2: Update the decisions index

**Files**: `apps/native-rd/docs/decisions/index.md`
**Commit**: same commit as Step 1 (D8 — atomic)
**Changes**:

- [x] Ledger: add a new row for ADR-0014 (Decision: "Containment is informative in both directions — the app offers, never asserts, never refuses"; Status: matches ADR file's Status line; Last Verified: `2026-08-04`).
- [x] Ledger: edit ADR-0013's row — append ", Q9's reopened cascade-up permission settled by [ADR-0014](./ADR-0014-containment-offers-never-refuses.md)" to its existing Status cell (D6). Do not touch its "Accepted 2026-07-26" fact.
- [x] Current positions: edit the Phase B row's "Current reference" cell to "[ADR-0013](./ADR-0013-phase-b-consolidated-position.md), [ADR-0014](./ADR-0014-containment-offers-never-refuses.md)" (D7).
- [x] Current positions: append a short clause to the Phase B row's "Current position" prose noting containment cascade (parent↔sub-step) is settled offer-only, never automatic, in both directions.

### Step 3: Format and validate

**Files**: both files above
**Commit**: none (validation only, folds into Step 1/2's commit)
**Changes**:

- [x] Run `bun run format:check` (or `bun run format` to auto-fix) — the only docs CI gate (`ci-docs.yml`).
- [x] Re-read every `file:line` citation in the new ADR against current `queries.ts`/ADR files one more time before commit (lines drift fast in this repo — the issue's own citations for `queries.ts` were re-verified this session and matched, but the ADR/index.md line numbers referenced _within the new ADR_ should be spot-checked once more after final edits, since editing index.md shifts nothing but editing sibling ADRs never happens here — low risk, cheap check).
- [x] Confirm no `apps/native-rd/src/` file was touched.

## Testing Strategy

- Not applicable in the unit/integration sense — this is a docs-only change.
- [x] Manual: read the finished ADR top-to-bottom and confirm every Intent Verification checkbox above is independently satisfiable by a reviewer without opening `step-containment-semantics.md`.
- [x] Manual: confirm `index.md`'s Current-positions table alone (no Ledger-diffing) tells a reader that Q9's cascade-up is settled offer-only — per the "Reading rule."
- [x] `bun run format:check` passes.

## Not in Scope

| Item                                                                                         | Reason                                                                                   | Follow-up                                            |
| -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| Implementing the resolver predicate split (`invite` vs. `parked`)                            | That's #536 (F2) — sequenced after this ADR                                              | #536                                                 |
| Wiring `resolveFocusStepId`'s discarded `kind`/`parentIndex`/`childCount` into Focus Mode UI | That's #537 (F3) — sequenced after this ADR                                              | #537                                                 |
| The parent-completion "parts offer" modal (Keep/Set aside/Edit)                              | That's #538 (F6) — sequenced after this ADR                                              | #538                                                 |
| Fixing `areAllStepsComplete`'s stale docstring (dead `FocusModeScreen` reference)            | Named in the research as a trivial follow-up, not part of the ADR's content              | none filed yet — flag if picked up during #536       |
| Deciding substructure depth beyond one level (ADR-0013's open question)                      | Explicitly not forced by this ADR; depth stays a separate open question                  | none — tracked in ADR-0013's Open Questions Register |
| Building a soft-deleted-row restore path (`undeleteStep` etc.)                               | Named as a genuine gap in the research's Open Questions, unrelated to containment itself | none filed yet                                       |

No items deferred beyond the above — everything else in the issue's Acceptance list is in scope for Step 1.

## Discovery Log

- [2026-08-04] **D5/D6 corrected — superseded ADRs get a status-line pointer, and the plan omitted it.** The plan said ADR-0012 and ADR-0013 "stay unedited." Their _bodies_ do, but the header is explicitly the mutable surface (`index.md:3-9`) and ADR-0013's own Consequences require it: "Superseded ADRs carry a status-line pointer here; their bodies stay unedited" (`ADR-0013:98`). Every precedent confirms it — `ADR-0001`, `ADR-0006`, `ADR-0010`, `ADR-0011` all carry a forward pointer appended to their `**Status:**` line. Added one to ADR-0013 (direct, narrow supersession) and to ADR-0012 (whose "Reopens A-prototype Q9" consequence is the thing being closed — a reader landing there must not have to diff the chain, per the reading rule at `index.md:11-13`). Bodies untouched in both.
- [2026-08-04] **Superseded target narrowed from "the Q9 cascade-up permission" to "the reopened A-prototype Q9."** ADR-0013 does not restate the cascade-up permission as a standing decision — it lists Q9 in its Consequences' still-open register (`ADR-0013:97`) and names "parent-done arithmetic" only as a bookkeeping example (`:71`). What ADR-0014 closes is the _open question_, not a permission ADR-0013 granted. Wording in the `**Supersedes:**` line and `## Supersession` reflects that.
- [2026-08-04] **"Exactly four functions write `step.status`" made precise.** The research doc's phrasing (`step-containment-semantics.md:228`) is falsifiable as written: `createStep` (`queries.ts:670`) and `createSubStep` (`:728`) also write `status:`, setting the initial `pending` on insert. The ADR says four functions **transition** an existing step's status, and names the two creates parenthetically as non-transitions. The argument is unaffected — no create path can produce the contested state.
- [2026-08-04] **Citation drift, one line.** `completeStep`'s evidence refusal is `queries.ts:860-866` (guard at `:860`). All others re-verified exact: signature `:854-858`, status writes `:882` / `:901` / `:924` / `:943`, declarations `:854` / `:896` / `:917` / `:938`, `updateStep` `:751` (no status write), orphan promotion `:591-608`, `GroupedStep` depth contract `:406-409`. ADR-0012's cascade-up example is at `:42` (the issue said `:41`); hand-editable override `:57` exact; Q9 reopening `:77-82`.
- [2026-08-04] **Status line is `Proposed — pending sign-off`** — dropped D4's parenthetical `(hitl)`, since no existing ADR status line references a GitHub label. On sign-off, four places flip together: ADR-0014's `**Status:**` line, its closing italic line, the index Ledger row, and the `(proposed 2026-08-04)` parenthetical in ADR-0012's and ADR-0013's pointers.
- [2026-08-04] **D9 reversed — all line-number citations stripped from the ADR.** D9 had the implement step carry the research doc's `file:line` citations into ADR-0014. That was wrong for this document class, and it broke immediately: adding the forward pointer to ADR-0013's header shifted its body +3 lines, invalidating ADR-0014's own `ADR-0013:97` / `:71` / `:55` cites within the same commit. Two facts settle it — (a) **no existing ADR cites anything by line**, not source (`ADR-0004`, `ADR-0012` reference `src/db/queries.ts` and `src/types/steps.ts` file-level only) and not sibling ADRs; (b) ADR bodies are immutable once accepted, so a line number is a claim the document can never correct. ADR-0014 now cites files + symbol names and sibling ADRs by `§ Section`, and carries a short header note saying why. The `completeStep` signature block and four-writer table are reproduced in full so the argument stays checkable without them. Verification shifts from "line still says X" to "symbol still exists" — all ten cited symbols confirmed present in `queries.ts`.
- [2026-08-04] **Four-direction table de-implemented.** It named `parked` as the all-set-aside resolver kind and the literal `↳ {{parent}} · part {{index}} of {{total}}` template. Neither is this ADR's to fix: resolver kinds today are `leaf | invite | flat | none`, `parked` exists only as a goal-level i18n namespace, and the research explicitly deferred the name (§Open questions, item 4 — "whether it reuses the literal resolver-kind name … is F2's call"). It also contradicted this ADR's own Consequences line disclaiming resolver-kind vocabulary. The Behaviour column now fixes only offer/assert/refuse/inform, with a note that the table is the constraint #536-#538 implement, not a description of the current screen (only the cascade-up invite exists, unmounted).
- [2026-08-04] **Added a durable-vs-contingent note to the three mechanical reasons.** Reasons 1 and 3 describe current code and a refactor could expire them; reason 2 (CRDT) holds as long as ADR-0003's sync layer does. Stated explicitly so a future reader who finds 1 or 3 stale does not read the decision as reopened.
- [2026-08-04] **Research doc undercount corrected.** The research says three soft-delete writers (`:274`, `:959`, `:1295`); there are four — `badge` at `:1537` was missed. The ADR avoids the count entirely ("every delete … sets an `isDeleted` flag, and there is no restore path"), which is both true and stable. Restore-path absence re-verified: zero matches for `undelete|restoreStep|restoreGoal|isDeleted: null`.
- [2026-08-04] **ADR-0012's Ledger row left as-is.** It already routes to ADR-0013, whose row now names ADR-0014 — the same one-hop shape as `ADR-0010`'s row, which points only at ADR-0013 and skips the intermediate ADR-0011/0012. The header pointer on the ADR-0012 file itself carries the detail.
