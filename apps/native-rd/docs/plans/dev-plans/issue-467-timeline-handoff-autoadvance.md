# Development Plan: Issue #467

## Issue Summary

**Title**: [Integrate] Focus Mode rebuild 2/2 — Timeline handoff + auto-advance states
**Type**: feature (screen behavior + navigation contract completion)
**Complexity**: MEDIUM (upper end, close to the ~500-line PR target — matches the issue's own ~450–700 LOC estimate)
**Estimated Lines**: ~500–650 lines (navigation contract + screen resolution rewrite + two new screen states + a sizable test-suite update, but **zero new i18n copy or new `db/queries.ts` logic** — both are shipped and unused today)

## Intent Verification

- [x] Tapping a node (root or sub-step) in `TimelineJourneyScreen` navigates to `FocusMode` with that node's own `stepId`, and `FocusModeScreen` renders **that step's own card** (in its current status — in-progress/paused/completed), not whatever `resolveNextActionableStep` would otherwise pick.
- [x] Tapping "Set this step aside", "Pick this back up", "✓ Mark complete", or "Reopen this step" no longer leaves the screen on the same step: the card immediately reflects the freshly-recomputed next actionable step (leaf, invite, or flat), skipping paused/completed rows, exactly as `resolveNextActionableStep` already resolves for `GoalsScreen`/`TimelineJourneyScreen`.
- [x] Completing the last pending sub-step of a parent renders the **parent's own card** next (the resolver's existing `{ kind: "invite" }` result, now reachable because the screen re-resolves after the mutation instead of holding the child's id).
- [x] A goal with at least one pending/in-progress step, at least one paused step, and no actionable step renders `FocusParkedState` ("Nothing in progress." / "`{N}` set aside") with one resumable row per paused step; tapping a row's "resume ›" resumes that row's step and the screen then shows whichever step becomes actionable.
- [x] A goal whose every step is `completed` renders `FocusCurrentTaskCard`'s `all-complete` state ("Every step done." + trophy callout); tapping "Design your badge" navigates to `CompletionFlow` with `{ goalId }` — the same route `TimelineJourneyScreen`'s `FinishLine.onBadgePress` already uses.
- [x] A goal with zero steps still renders chrome-only (header + `0 / 0 done` strip, no card, no parked state) — the existing `renders nothing but chrome when the goal has no steps` regression stays green untouched.
- [x] `FocusProgressStrip` ("`{done}` / `{total}` done · See all steps ›") stays mounted above the card/parked/all-done body in every state, matching the canonical `Focus Mode A Prototype.dc.html` markup (the strip's own `<div>` wraps every `sc-if` body branch, not just the active-step one).

## Dependencies

| Issue | Title                                                                           | Status                   | Type                                                                |
| ----- | ------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------- |
| #466  | [Integrate] Focus Mode rebuild 1/2 — strip old chrome + mount Current Task Card | ✅ Met (merged, PR #524) | Blocker (this issue is its sibling/slice 2/2)                       |
| #449  | [Integrate] Finishing flow — retire the old completion path                     | Open                     | Blocked-by-this (its CTA entry point is #467's "Design your badge") |
| #519  | refactor(i18n): collapse badgeI18nKey / stateWordI18nKey                        | Open                     | Blocked-by-this (already lists #466 + #467)                         |
| #346  | refactor(focus): extract useFocusMode hook from FocusModeScreen                 | Open                     | Soft (touches the same state plumbing; sequence after)              |
| #523  | Goal-level evidence has no entry point after the Focus Mode rebuild             | Open                     | Related, not a dependency                                           |
| #526  | Focus Mode: set-aside / pick-back-up fire no screen-reader announcement         | Open                     | Related (`dep:blocked` names this issue as its blocker)             |

**Status**: ✅ All dependencies met. #466 is closed and merged (PR #524, 2026-07-27); the issue body's own "Reopened 2026-07-29" note and a direct read of `FocusModeScreen.tsx:140-147` / `navigation/types.ts:27` confirm the scope below is genuinely unshipped. #449/#519/#346 are downstream of this issue, not blockers on it.

## Objective

Close the two seams #466 deliberately deferred (its D1 and D2): replace the "resolve once and hold `currentStepId`" model with a reactive re-resolve so completing/pausing/resuming/reopening a step auto-advances to the next actionable one (or to the parent once all its children are done), and give `TimelineJourneyScreen`'s node taps a `stepId` so the return leg lands Focus Mode on the tapped step instead of wherever the resolver would otherwise land. Mount the two screen states #466 built but never wired: `FocusParkedState` (all remaining work set aside) and `FocusCurrentTaskCard`'s `all-complete` variant (every step done), replacing #466's placeholder D10 fallback (first-paused-or-last-step) with the real states.

## Decisions

| ID  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Alternatives Considered                                                                                                                                                                                                                         | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | **Drop the "held `currentStepId`" state entirely for the unpinned case.** With no `stepId` route param, `currentStepId` becomes a pure derivation — `resolveFocusStepId(stepRows)` — recomputed on every render from the live `useQuery` rows, with no `useEffect` needed to detect staleness.                                                                                                                                                                                                                                                                                                                                                                                      | (a) Keep the effect-based "hold, then re-resolve only when the id vanishes" model and add a separate "trigger re-resolve" call after each mutation; (b) a version counter bumped by each handler                                                | #466's D1 explicitly named this issue as the one that undoes the hold ("#467 replaces the resolve-once-and-hold with a re-resolve after each state change" — `issue-466-...md` line 170). A pure derivation is strictly simpler than a version-counter/effect scheme and is already the exact mechanism the existing "re-resolves when the held step is gone from the rows" test (`FocusModeScreen.test.tsx:617-635`) exercises via `rerender` — proving the test harness treats "the query re-emits" and "recompute" as the same event, so no extra plumbing is needed to make auto-advance observable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D2  | **The Timeline-return leg is a one-shot "pin" layered on top of D1's pure derivation**, not a replacement for it: `currentStepId = (pinnedStepId is present in stepRows) ? pinnedStepId : resolveFocusStepId(stepRows)`. The pin is populated from `route.params.stepId` via `useFocusEffect` (not a `useState` initializer) and cleared by calling `navigation.setParams({ stepId: undefined })` in the same effect, then cleared a second way — any successful step-toggle mutation (`pauseStep`/`resumeStep`/`completeStep`/`uncompleteStep`) also clears it — so the pin survives exactly until the user either leaves and returns via a fresh tap, or acts on the pinned step. | (a) A `useState(() => route.params.stepId ?? null)` lazy initializer; (b) a plain `useEffect(() => {...}, [route.params.stepId])`                                                                                                               | (a) only runs once, on first mount. `FocusModeScreen` is already mounted and sitting _underneath_ `TimelineJourneyScreen` in the stack when a node is tapped — `navigation.navigate("FocusMode", ...)` pops back to that existing instance rather than remounting it, so a lazy initializer would silently miss every return-leg tap after the first. (b)'s dependency array is value-compared: tapping the _same_ node twice in a row (e.g. re-checking a step after acting on a different one) wouldn't re-fire, since `route.params.stepId` didn't change between the two navigations. `useFocusEffect` fires on every focus transition regardless of value equality, matching the "consume a value on (re-)arrival" pattern already used for exactly this class of problem — `pendingDesignStore.consume(goalId)` inside `useFocusEffect` in `CompletionFlowScreen.tsx:810-815`. Clearing via `setParams` (this repo's first use of it, but the pattern is a direct extension of `NewGoalModal.tsx:20`'s route-scoped `useNavigation<NativeStackNavigationProp<GoalsStackParamList, "NewGoal">>()` typing) is the "consume" half — without it, navigating back to Focus _again_ later via some unrelated path (not through Timeline) would silently re-pin to the stale step and undo whatever auto-advance happened meanwhile. |
| D3  | `TimelineStep`'s `onNodePress` contract changes from `(stepIndex: number) => void` to `(stepId: string) => void`, and the root node calls `onNodePress(step.id)` while `ChildRow` calls `onNodePress(child.id)` — **not** `onNodePress(parentIndex)`, which is what `ChildRow` passes today (`TimelineStep.tsx:186`).                                                                                                                                                                                                                                                                                                                                                               | (a) Keep index-based `onNodePress` and have `TimelineJourneyScreen` map the index back to an id via `stepsWithChildren[index].id`; (b) add a second `onSubStepNodePress` prop just for children                                                 | `TimelineStepData`/`TimelineStepChild` already carry their own `id` (`TimelineStep.tsx:14, :44`) — the exact identifier the new route param needs — so passing it directly needs no index-to-id lookup layer. Rejecting (a): today's `ChildRow` passing `parentIndex` for a sub-step tap is itself a latent bug (a tapped sub-step reports its _parent's_ position), invisible only because the current handler ignores its argument entirely (`TimelineJourneyScreen.tsx:196-198`); fixing the return leg is the first caller that would expose it, so it must be fixed in the same commit rather than carried forward. Rejecting (b): one callback for both root and child nodes matches the existing one-`onNodePress`-prop shape and needs no new prop threaded through `TimelineStep`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D4  | Add `stepId?: string` to `GoalsStackParamList["FocusMode"]` (`navigation/types.ts:27`) — optional, not required.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Making it required and having every caller pass `undefined` explicitly                                                                                                                                                                          | Four other call sites navigate to `FocusMode` with `{ goalId }` only (`CompletionFlowScreen.tsx:431`, `EditModeScreen.tsx:444`, `GoalsScreen.tsx:189,191`, `TimelineJourneyScreen.tsx:193` — the "Back to Focus" leg, unaffected by this issue). An optional field matches the existing `EditMode: { goalId; cameFromFocus?: boolean }` and `TimelineJourney: { goalId; originBadgeId?: string }` precedents in the same file, both additive optional params on a route many callers reach without the new field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D5  | Distinguish all-paused from all-done by checking `areAllStepsComplete(stepRows)` **first**; only fall to `FocusParkedState` when that's false. Both states share the same underlying resolver signal (`resolveNextActionableStep` returns `{ kind: "none" }` for _both_ "every step completed" and "every remaining step paused/mixed-with-completed"), so the screen needs its own tie-breaker.                                                                                                                                                                                                                                                                                    | Adding a new discriminant to `resolveNextActionableStep`'s return type (e.g. splitting `"none"` into `"none-done"` / `"none-parked"`)                                                                                                           | `areAllStepsComplete` (`db/queries.ts:379-382`) already exists, is already imported by `TimelineJourneyScreen` for exactly this same distinction (feeding `FinishLine`'s `allStepsComplete` prop), and is a pure one-line boolean — no reason to grow the resolver's contract (which `GoalsScreen` also depends on) for a distinction only this screen needs to make.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D6  | Zero-step goals keep rendering chrome-only (no card, no parked state, no all-done state) — checked via an explicit `stepRows.length === 0` branch **before** the `areAllStepsComplete` check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Letting `areAllStepsComplete([])` decide it (it already returns `false` for an empty array, so this would fall to `FocusParkedState` with zero rows — "Nothing in progress. 0 set aside", a nonsensical render for a goal with no steps at all) | Pins the existing `renders nothing but chrome when the goal has no steps` test (`FocusModeScreen.test.tsx:378-389`) unchanged — that test is exactly this edge case and must not regress.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D7  | Resuming a row from `FocusParkedState` reuses `runStepMutation`/`resumeStep` through a new **explicit-id** handler, `handleResumeStep(stepId: string)`, rather than reusing `handlePickUp` (which reads `currentStep.id` — `FocusModeScreen.tsx:313-318` — and is meaningless here since there is no `currentStep` while the parked state is showing). `handlePickUp` itself is left as the pinned/actionable-step case's handler, now implemented as `handleResumeStep(currentStep.id)`.                                                                                                                                                                                           | Giving `FocusParkedState`'s rows their own inline `resumeStep(row.id)` calls, bypassing `runStepMutation`'s toast/Sentry/kind wiring                                                                                                            | Every other mutation path in the screen goes through `runStepMutation` for consistent failure surfacing (toast + `reportError`); a parked row failing silently on a write error would be the one path that doesn't.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D8  | The "Design your badge" CTA (`FocusAllCompleteCardProps.onDesignBadge`) navigates to `navigation.navigate("CompletionFlow", { goalId })` — the exact call `TimelineJourneyScreen.tsx:207-209`'s `handleBadgePress` already makes for `FinishLine`'s equivalent CTA.                                                                                                                                                                                                                                                                                                                                                                                                                 | Navigating to some new/placeholder route pending #449's redesign                                                                                                                                                                                | #449 (open) is scoped to _retire the old completion path_, not to invent a new route — `CompletionFlow` is the one existing target for "start finishing this goal" from every other surface (`FinishLine`, and `CompletionFlowScreen.tsx:431`'s own re-navigation to itself after a `replace`). Wiring to anything else would create a second, divergent finishing entry point for #449 to reconcile later.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D9  | Mounting `FocusParkedState` wraps it in a `ScrollView` at the call site in `FocusModeScreen.tsx` (new `parkedScrollContent` style), rather than inside `FocusParkedState` itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Adding a `ScrollView` inside `FocusParkedState.tsx`                                                                                                                                                                                             | `FocusParkedState`'s `rows` container (`FocusParkedState.styles.ts:37-39`) is a plain `gap`-only `View` with no scroll handling — fine for the handful of rows shown in its own Storybook stories/tests, but the component is already shipped and storied (#450); per #466 D5's precedent ("failure mode of extending an already-shipped, storied component" — not literally cited there for this component, but the same principle applies), the caller accommodates an unbounded row count rather than modifying the component's internals for a scenario `FocusParkedState`'s own contract doesn't promise to handle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D10 | `FocusModeScreen` keeps its own `useNavigation<NativeStackNavigationProp<GoalsStackParamList, "FocusMode">>()` call (route-scoped, replacing the current unscoped `NavigationProp<GoalsStackParamList>` at `FocusModeScreen.tsx:9,102`) instead of accepting a `navigation` prop from the outer `FocusModeScreen` function.                                                                                                                                                                                                                                                                                                                                                         | Passing the outer component's already-correctly-typed `FocusModeNavProps["navigation"]` down into `FocusContent` as a prop                                                                                                                      | `FocusModeScreen.test.tsx`'s `routeProps.navigation` fixture is `{} as never` (`FocusModeScreen.test.tsx:283`) — the suite relies entirely on the globally mocked `useNavigation()` hook (`jest.mock("@react-navigation/native", ...)`, `FocusModeScreen.test.tsx:16-29`) for every `navigate`/`goBack` assertion today. Switching to prop-based navigation would require every existing test in the file to also thread a working navigation object through `routeProps`, a large, unrelated churn. Route-scoping the existing hook call is a one-line generic-parameter change (mirroring `NewGoalModal.tsx:20`) that adds `setParams` typing without touching the test infrastructure — `useNavigation`'s generic type parameter is erased at runtime, so the existing mock continues to satisfy it unchanged (the mock's `setParams` must still be added, see Step 2).                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D11 | **No `AccessibilityInfo` announcement for the child→parent transition** — completing the last sub-step of a parent fires only the existing `focusMode:a11y.stepCompleted` announcement for the sub-step itself, exactly as any other completion does. The auto-advance to the parent's card is silent to screen readers, same as every sibling→sibling advance this issue introduces. Deferred to **#526**, which owns Focus Mode's screen-reader-announcement gaps end to end.                                                                                                                                                                                                     | (a) Add a new `focusMode:a11y.movedToParent` key announced on the child→parent jump; (b) announce every auto-advance, parent or sibling                                                                                                         | **User decision (2026-07-29)**, on the open question the issue body itself deferred ("a decision on whether that transition deserves its own a11y announcement"). Not code-answerable: `focusMode:a11y` has exactly four keys — `carousel`, `allStepsComplete`, `stepCompleted`, `stepUncompleted` (`src/i18n/resources/en/focusMode.json`) — none covering a context jump, so either answer required new copy in `en` + `de`. Choosing #526 keeps every Focus Mode announcement gap in one issue with one consistent copy voice, rather than landing a single ad-hoc key here and leaving the sibling→sibling case (the far more frequent one) silent beside it. This issue therefore ships **no** new `announceForAccessibility` calls beyond the ones already in `runStepMutation`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Affected Areas

- `apps/native-rd/src/navigation/types.ts`: add `stepId?: string` to `GoalsStackParamList["FocusMode"]` (D4).
- `apps/native-rd/src/components/TimelineStep/TimelineStep.tsx`: `onNodePress` contract changes from index to id (D3); `ChildRow` drops its now-unused `parentIndex` prop.
- `apps/native-rd/src/components/TimelineStep/__tests__/TimelineStep.test.tsx`: update the two `onNodePress` assertions for the new id-based contract.
- `apps/native-rd/src/screens/TimelineJourneyScreen/TimelineJourneyScreen.tsx`: `handleNodePress(stepId: string)` navigates with `{ goalId, stepId }` instead of ignoring its argument and passing `{ goalId }` only (`TimelineJourneyScreen.tsx:196-198`).
- `apps/native-rd/src/screens/TimelineJourneyScreen/__tests__/TimelineJourneyScreen.test.tsx`: update the "step node press navigates to FocusMode" assertion to include `stepId: "step-1"`.
- `apps/native-rd/src/screens/FocusModeScreen/FocusModeScreen.tsx`: the core rewrite —
  - Replace the held-`currentStepId` effect (`FocusModeScreen.tsx:123, :140-147`) with the pure-derivation + pin model (D1/D2).
  - Route-scope the `useNavigation` generic (D10) and add the `useFocusEffect`-driven pin-consume/clear.
  - Add the all-paused/all-done render branches (D5/D6) replacing the current `currentStep ? ... : null` fallthrough (`FocusModeScreen.tsx:442-471`).
  - Add `handleResumeStep(stepId)` (D7) and `handleDesignBadge` (D8).
  - Update the doc comment at `FocusModeScreen.tsx:81-98` (`resolveFocusStepId`) — its D10 paused/last-step fallback goes away; the real states replace it.
- `apps/native-rd/src/screens/FocusModeScreen/FocusModeScreen.styles.ts`: add a `parkedScrollContent` style for the `FocusParkedState` mount (D9).
- `apps/native-rd/src/screens/FocusModeScreen/__tests__/FocusModeScreen.test.tsx`: mock updates (`useFocusEffect`/`setParams` on the local `useNavigation` override, `areAllStepsComplete` added to the `../../../db` mock) plus the test additions/rewrites in the Implementation Plan and Testing Strategy below.
- No changes expected to `src/db/queries.ts` (`resolveNextActionableStep`/`areAllStepsComplete` already have the exact shape this issue needs), `src/components/FocusParkedState/*`, or `src/components/FocusCurrentTaskCard/*` (all consumed as-shipped).

## Implementation Plan

### Step 1: Timeline node-tap carries the step's own id

**Files**: `navigation/types.ts`, `TimelineStep.tsx`, `TimelineJourneyScreen.tsx`, their `__tests__`
**Commit**: `feat(timeline): carry stepId on node-tap navigation to FocusMode`
**Changes**:

- [x] Add `stepId?: string` to `GoalsStackParamList["FocusMode"]` with a doc comment (mirroring `originBadgeId`'s) explaining it's set only by the Timeline-return leg (D4).
- [x] Change `TimelineStepProps["onNodePress"]` to `(stepId: string) => void`; the root `TimelineNode`'s `onPress` calls `onNodePress(step.id)` (`TimelineStep.tsx:83`).
- [x] `ChildRow` calls `onNodePress(child.id)` instead of `onNodePress(parentIndex)`; drop the now-unused `parentIndex` prop from `ChildRow`'s props and its call site in `TimelineStep` (D3).
- [x] `TimelineJourneyScreen.handleNodePress(stepId: string)` navigates `navigation.navigate("FocusMode", { goalId, stepId })` (`TimelineJourneyScreen.tsx:196-198`).
- [x] Update `TimelineStep.test.tsx`'s two `onNodePress` assertions: root-node press expects `toHaveBeenCalledWith("step-1")` (the fixture's `baseStep.id`), sub-step press expects `toHaveBeenCalledWith(<child's own id>)` — update the test's title away from "with the parent index" since that's the bug being fixed.
- [x] Update `TimelineJourneyScreen.test.tsx`'s "step node press navigates to FocusMode" to expect `{ goalId: "goal-1", stepId: "step-1" }` (the default fixture's first step).

### Step 2: Auto-advance — drop the held id, land the Timeline return leg

**Files**: `FocusModeScreen.tsx`, `FocusModeScreen.test.tsx`
**Commit**: `feat(focus-mode): auto-advance after step-toggle mutations, land Timeline returns on the tapped step`
**Changes**:

- [x] Route-scope `useNavigation` to `NativeStackNavigationProp<GoalsStackParamList, "FocusMode">` (D10).
- [x] Replace the `currentStepId` state + resolve-on-mount effect (`FocusModeScreen.tsx:123, :140-147`) with: `resolveFocusStepId(stepRows)` as a pure `useMemo`-free derivation (D1), a `pinnedStepId` state seeded to `null`, and a `useFocusEffect` that — when `route.params.stepId` is set — calls `setPinnedStepId(route.params.stepId)` and `navigation.setParams({ stepId: undefined })` in the same tick (D2).
- [x] `currentStepId = pinnedStepId && stepRows.some(r => r.id === pinnedStepId) ? pinnedStepId : resolveFocusStepId(stepRows)`.
- [x] In `runStepMutation`, on a successful `"step-toggle"` mutation, also call `setPinnedStepId(null)` (in addition to firing the existing announcement) — this is what turns "landed here via Timeline" into "now following auto-advance" the moment the user acts on the pinned step.
- [x] Remove the D10 paused/last-step fallback from `resolveFocusStepId`'s doc comment and body — it now returns `null` on `{ kind: "none" }` with no fallback; the render branch (Step 3/4) owns what to show instead.
- [x] Rewrite the `current-step resolution while mounted` describe block (`FocusModeScreen.test.tsx:591-636`): the "holds the same step when its status changes under it" test (`:592-615`) asserted the _opposite_ of this issue's scope and must be replaced with an "auto-advances to the next actionable step once the held step completes" test using the same fixture/`rerender` idiom, expecting `currentCardTitle()` to flip from "Read docs" to "Practice". The "re-resolves when the held step is gone from the rows" test (`:617-635`) stays conceptually valid under pure derivation — keep it, retitled to drop the "held" framing.
- [x] Add a dynamic sub-step→parent test: start from `PARTIAL_LEAF_STEPS`-shaped data (Drill A completed, Drill B pending, `Practice` as parent), fire `handleMarkComplete` (via the "✓ Mark complete" button) on Drill B, `rerender` with Drill B now completed, and assert `currentCardTitle()` becomes "Practice" — the explicit dynamic regression the issue calls out (distinct from the existing static-mount `focuses %s` resolution table at `:955-967`, which only proves the resolver's answer on first render, not that a live transition reaches it).
- [x] Add a return-leg test: render `FocusModeScreen` with `routeProps.route.params = { goalId: "goal-1", stepId: "step-2" }` against fixture data where the resolver would otherwise pick `step-1`, and assert `currentCardTitle()` is `step-2`'s title.
- [x] Add a pin-then-act test: same return-leg setup landing on a _paused_ step-2, fire "Pick this back up", and assert the card no longer requires the pin to keep showing step-2 (i.e. a subsequent `rerender` with step-1 completed now flips to whatever the resolver picks, proving the pin cleared).
- [x] Extend the local `useNavigation` mock override in this file with `setParams: mockSetParams` (a new top-level `jest.fn()`) alongside the existing `goBack`/`navigate` overrides, since `../../../__tests__/mocks/navigation.ts`'s shared `useNavigation` stub has no `setParams`.

### Step 3: All-paused screen state

**Files**: `FocusModeScreen.tsx`, `FocusModeScreen.styles.ts`, `FocusModeScreen.test.tsx`
**Commit**: `feat(focus-mode): mount FocusParkedState when nothing is actionable`
**Changes**:

- [x] Import `FocusParkedState` and `areAllStepsComplete`.
- [x] Add the render branch: `currentStep` present → existing card logic; else `stepRows.length === 0` → render nothing (D6, unchanged); else `areAllStepsComplete(stepRows)` → Step 4's all-done card; else → `FocusParkedState` wrapped in a `ScrollView` (D9) fed `rows = stepRows.filter(s => s.status === StepStatus.paused).map(s => ({ id: s.id, title: s.title ?? "", onResume: () => handleResumeStep(s.id) }))`.
- [x] Add `handleResumeStep(stepId: string)` (D7): `runStepMutation("step-resume", "step-toggle", () => resumeStep(stepId as StepId))`; refactor `handlePickUp` to call it with `currentStep.id`.
- [x] Add the `parkedScrollContent` style (D9).
- [x] Tests: a goal with one pending-but-paused-first step (all remaining paused, none actionable) renders `FocusParkedState`'s heading/count; tapping a row's resume label calls `resumeStep` with that row's id; a goal with a mix of one completed + N paused steps (none actionable, not all complete) also renders the parked state, not the all-done one (pins D5's ordering).

### Step 4: All-done screen state

**Files**: `FocusModeScreen.tsx`, `FocusModeScreen.test.tsx`
**Commit**: `feat(focus-mode): mount the all-complete card and wire Design your badge`
**Changes**:

- [x] Render `FocusCurrentTaskCard status="all-complete" title={goal.title ?? ""} onDesignBadge={handleDesignBadge}` when `areAllStepsComplete(stepRows)` is true (D5, ordered before the parked-state check per Step 3).
- [x] Add `handleDesignBadge`: `navigation.navigate("CompletionFlow", { goalId })` (D8).
- [x] Tests: a goal whose every step is `completed` renders the "Every step done." heading and the trophy callout body; tapping "Design your badge" calls `mockNavigate` with `("CompletionFlow", { goalId: "goal-1" })`.

## Testing Strategy

- [x] Unit tests for `TimelineStep`, `TimelineJourneyScreen`, and `FocusModeScreen` (Jest 30, `@testing-library/react-native` v13), per the Implementation Plan steps above.
- [x] Test files stay at their existing paths (`src/components/TimelineStep/__tests__/`, `src/screens/TimelineJourneyScreen/__tests__/`, `src/screens/FocusModeScreen/__tests__/`).
- [x] Use `test.each`/the existing `it.each` resolution table (`FocusModeScreen.test.tsx:955-967`) for any additional static-mount fixture cases; use the `rerender`-after-`setupQueries` idiom already established at `:592-635` for every _dynamic_ (post-mutation) assertion, since that is what the mocked `useQuery` supports.
- [x] Before deleting the "holds the same step when its status changes under it" test (Step 2), confirm no other suite pins the old hold behavior — grep for `#466 D1` / "resolve-once-and-hold" outside this file first (per `AGENTS.md`'s "grep before deleting a test" rule); expected to find none, since #466's own test suite (already merged) never asserted this from the Timeline/TimelineStep side.
- [ ] Manual testing: `npx expo run:ios` — (a) open a 3-step goal, tap "See all steps ›", tap a _different, non-current_ node in Timeline, confirm Focus lands on that exact step; (b) from there, tap the same node again on a second visit (repeat-tap case D2 is designed for) and confirm it still lands correctly; (c) mark the current step complete and confirm the card advances without navigating away from Focus Mode; (d) set aside every remaining step and confirm the parked state appears with correct resumable rows; (e) complete every step and confirm the all-done state appears and "Design your badge" opens `CompletionFlow`; (f) complete the last sub-step of a parent and confirm focus lands on the parent.

## Not in Scope

| Item                                                                                                                           | Reason                                                                                                                                                                                                       | Follow-up                         |
| ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------- |
| The outbound Focus→Timeline leg (`handleTimelineTap`) passing the current step's id so Timeline could auto-scroll/highlight it | Issue text scopes this issue to "the _return_ leg" only ("The _outbound_ leg already landed in #466... What is still owed is the _return_ leg") — the outbound call is explicitly unchanged in #466's own D2 | none filed — flag if wanted later |
| An `AccessibilityInfo` announcement specifically for the child→parent transition                                               | **Decided: left to #526** (D11). The issue deferred this judgment call; the answer is to keep every Focus Mode announcement gap in one issue rather than land one ad-hoc key here                            | #526                              |
| Announcing auto-advance itself (e.g. "Now on: {next step title}") for the sibling→sibling case                                 | Not requested by the issue body, and D11 puts the child→parent case in #526 too — so both auto-advance announcement gaps land there together                                                                 | #526                              |
| Set-aside/pick-back-up screen-reader announcements in general                                                                  | Explicitly #526's own remit (`dep:blocked` names this issue as its blocker, meaning #526 proceeds once this lands, not that this issue must also fix it)                                                     | #526                              |
| Redesigning `CompletionFlowScreen` itself                                                                                      | Explicitly #449's scope ("Finishing flow — retire the old completion path"); this issue only wires the existing route as the all-done CTA's target (D8)                                                      | #449                              |
| Goal-level evidence's missing entry point                                                                                      | #466's D9/#523's remit, unrelated to Timeline handoff or auto-advance                                                                                                                                        | #523                              |
| Extracting `useFocusMode` out of `FocusModeScreen`                                                                             | #346's own remit; explicitly sequenced _after_ this issue in #467's "Blocks" list, since it rewrites the same state plumbing this issue changes                                                              | #346                              |

## Open Questions

**None open.** The one genuine judgment call — whether the child→parent transition gets its own `AccessibilityInfo` announcement — was answered by the user on 2026-07-29: **leave it to #526**. Recorded as **D11**. Nothing else blocks implementation.

## Follow-ups found during implementation (2026-07-29, not in this PR's scope)

Surfaced by manual evaluation: a goal with parent "step 1" + sub-steps a/b, all three paused, then the **parent alone** resumed. Focus offered the parent as the next action with no mention of a or b. Four distinct defects, none covered by any open or closed issue:

| #   | Defect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Evidence                                                                                                                                  | Notes                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | **Nothing gates a parent step on its children.** `completeStep` (`db/queries.ts:854`) gates only on `canCompleteStep(plannedEvidenceTypesJson, stepEvidence)` — evidence. No completion path references `parentStepId` at all, so a parent completes with every sub-step still pending; the parent/child hierarchy is decorative. `resolveNextActionableStep`'s docstring states this is intentional ("step completion is per-step, not cascaded") but records no rationale.                             | `db/queries.ts:334-349`, `:854-889`                                                                                                       | The product decision to settle first: **require** every child `completed`, or **cascade** parent-completion down? Recommend _require, never cascade_ — matches #417's goal-level rule (a paused step blocks goal completion) and never marks work done that nobody did. Touches `canCompleteStep`'s signature (callers: `FocusModeScreen`, `StepCard`), `completeStep`'s throw, and F2 below. |
| F2  | **A paused child counts as satisfied.** The "any child still pending?" test excludes `completed` **and** `paused`, so pausing every child makes the parent's subtree read as finished and the resolver returns `{ kind: "invite" }` on the parent. Set-aside ≠ done. This is the direct cause of the trap above.                                                                                                                                                                                         | `db/queries.ts:617-620`                                                                                                                   | Shared by `GoalsScreen` / `TimelineJourneyScreen` / `FocusModeScreen`, so any change needs regression cases beside the #337 leaf/invite/flat set.                                                                                                                                                                                                                                             |
| F3  | **The invite card never says it is a parent.** The resolver returns `childCount` specifically so the card can show an "all N substeps done" readout (its own docstring). `FocusModeScreen` discards the resolver's `kind` (`resolveFocusStepId` returns only an id), so a parent renders identically to a flat step — no sub-step context, no route to the children. No `FocusCurrentTaskCard` prop and no `focusMode:` i18n key exist for it, so this is component + copy work, not just screen wiring. | `FocusModeScreen.tsx:91-95`; `FocusCurrentTaskCard.types.ts` (no parent/sub-step field); `i18n/resources/en/focusMode.json` (no such key) | Related to #435 (TimelineStep/StepCard fidelity) but distinct — that issue is glyphs and chip color.                                                                                                                                                                                                                                                                                          |
| F4  | **Timeline rows carry no step actions.** `TimelineJourneyScreen` calls no `resumeStep`/`pauseStep`/`completeStep`/`uncompleteStep` — it is read-only plus navigation, so the one surface that shows every step at once can't act on any of them. Deliberate per #417 ("the Set aside / Pick back up **button** is wired in the Focus rebuild … the Timeline 'set aside' **count** lands in #378"), but that split is what forces a hop through Focus to resume anything.                                 | `TimelineJourneyScreen.tsx` (grep: no step mutations)                                                                                     | Reverses #417's explicit decision, so it needs sign-off, not just a patch. Would also widen #526 (set-aside/pick-back-up screen-reader announcements) to a second surface.                                                                                                                                                                                                                    |

**Escape hatches that do exist after this PR** (both reachable in the trap above, neither obvious): tap the sub-step's own node in the Timeline → Focus pins to it → "Pick this back up" (impossible before this PR — the node tap ignored which node was hit); or complete the parent, at which point nothing is actionable and the parked state lists a and b as resumable rows. The second is backwards — it finishes the parent before its children, which is F1.

## Discovery Log

- [2026-07-29 Step 2] **The paused and completed card variants became pin-only, which the plan did not anticipate.** `resolveNextActionableStep` skips both statuses, so once `resolveFocusStepId` stopped falling back (D1), nothing unpinned can ever resolve to a paused or completed step — those two variants are now reachable _only_ via the Timeline pin. That is exactly the intended behavior (Intent #2: acting on a step must not leave the screen on it), but it invalidated five existing tests that reached those variants through a single-step goal on the plain route: the `step state mapping` `it.each` rows for `paused`/`completed`, "calls resumeStep from a paused card", "calls uncompleteStep from a completed card", and "announces the reopen only when the write succeeds". All five now render through a new `routePropsForStep(stepId)` helper (route params as the Timeline-return leg passes them) rather than being deleted, so the variants stay covered by the path that actually reaches them.
- [2026-07-29 Step 2] Two tests the plan expected Steps 3/4 to own had to be handled in Step 2 to keep every commit green: the D10-fallback tests ("falls back to a paused step when nothing is actionable" / "falls back to the last step when every step is done") assert behavior removed by Step 2 but replaced only in Steps 3/4. Step 2 rewrote them as interim "renders no step card when …" assertions; Step 3 replaced the paused one with the real `FocusParkedState` block, Step 4 replaced the all-done one with the real all-complete block. No commit is left without coverage of that branch.
- [2026-07-29 Step 3] The three no-actionable-step branches (D5/D6/D9) went into a module-level `NoActionableBody` component rather than inline in `FocusContent`'s JSX. The card section's status branch is already a 3-way nested ternary; adding three more levels inline would have made a 5-deep nest. `NoActionableBody` takes `stepCount`/`allStepsComplete`/`parkedRows` (plus Step 4's `goalTitle`/`onDesignBadge`) — booleans and rows, not the raw step array, so the D5/D6 ordering is stated once at its only call site.
- [2026-07-29 Step 3] Retitled "calls pauseStep and stays on the same step" → "calls pauseStep without navigating away". Its assertions were unchanged and still valid (`pauseStep` called, no `navigate`), but "stays on the same step" now describes the opposite of the shipped behavior.
- [2026-07-29 Step 4] `FocusAllCompleteCardProps` requires `title` but `AllCompleteView` never renders it (its heading is the fixed "Every step done." copy). Passed `goal.title ?? ""` as D8 implies; no change to the shipped component.
- [2026-07-29 Final] Manual on-device verification (Testing Strategy's last item) is **not** done — left unchecked. Everything it covers is asserted by the suite (return leg, repeat-tap consume, auto-advance, parked rows, all-done + CTA route, child→parent), but the visual read of the parked `ScrollView` (D9) and the all-done card in the real card section has not been eyeballed.
