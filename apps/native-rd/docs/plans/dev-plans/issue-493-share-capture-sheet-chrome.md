# Development Plan: Issue #493

## Issue Summary

**Title**: [Refactor] Share the wizard's evidence CaptureSheet chrome — retire Edit Goal's bespoke picker Modal
**Type**: refactor
**Complexity**: SMALL
**Estimated Lines**: ~200 lines (net; matches the issue's own ~150-250 LOC / 4-6 file estimate)

## Intent Verification

- [ ] Opening a step's or sub-step's evidence chip in `EditGoalView` shows the same reanimated slide-up + fading scrim as the New Goal wizard's capture sheet (verified in Storybook/device — Jest can't assert real transform/opacity timing).
- [ ] With Autism-Friendly theme or OS reduce-motion active, Edit Goal's evidence sheet appears/dismisses instantly (zero-duration timing via `useAnimationPref`), matching the capture sheet's existing behavior — it does not today.
- [ ] Pressing Android hardware back while Edit Goal's evidence sheet is open calls `onClose` (closes the sheet) instead of the OS Modal's back-press-pops-the-screen default — it does not today.
- [ ] Edit Goal's evidence sheet keeps its multi-select semantics unchanged: tapping an unselected type adds it, tapping the sole remaining selected type is a no-op (existing `EditGoalView.test.tsx` "refuses to deselect the last remaining type" case passes unmodified).
- [ ] `EvidenceTypePicker.stories.tsx`'s `CaptureSheet`, `CaptureSheetChangeScenario`, `AllThemesMatrix`, and `AuthoringAllThemes` stories render pixel-identical output to before the refactor (verified by `CaptureSheetBody`'s public prop signature and rendered structure staying untouched).
- [ ] `EditGoalView.styles.ts` no longer contains any `picker*` style keys; `EditGoalStepList.tsx` no longer imports `Modal` or `SafeAreaView`.
- [ ] `EvidenceTypePicker/index.ts`'s barrel still exports only `EvidenceTypePicker` + `EvidenceTypePickerProps` — no second public picker/shell added to the public surface (D7).

## Dependencies

| Issue | Title                                                          | Status                              | Type    |
| ----- | -------------------------------------------------------------- | ----------------------------------- | ------- |
| #384  | Epic: [parent epic]                                            | N/A                                 | Context |
| #489  | Extract EditGoalStepList — reusable step-editor row/list layer | ✅ Met (merged as #491, 2026-07-09) | Blocker |

**Status**: ✅ All dependencies met — `EditGoalStepList.tsx` already exists at `src/components/EditGoalView/EditGoalStepList.tsx` with the exact `Modal` block (`:460-501`) and `picker*` styles (`EditGoalView.styles.ts:379-434`) the issue describes; verified directly against current source, not from tracker claims.

## Objective

Retire `EditGoalStepList`'s hand-rolled `Modal`-based evidence-type-picker sheet in favor of the same animated, in-tree bottom-sheet chrome (`CaptureSheet`) that `EvidenceTypePicker mode="capture"` already uses in the New Goal wizard — so every evidence picker in the app shares one reanimated overlay/scrim/slide, one Android-back handler, one animation-pref-aware timing path, and one web-safe (non-`SafeAreaView`) bottom-inset treatment. The body content (single-select radio grid vs. multi-select checkbox grid) stays exactly as it is today on each side; only the surrounding chrome is shared.

## Decisions

| ID  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Alternatives Considered                                                                                                                                                                                                           | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | New exported component `AnimatedSheet` at `src/components/EvidenceTypePicker/AnimatedSheet.tsx`. Props: `visible`, `onClose`, `title: string`, `subLine?: string`, `closeLabel: string`, `closeTestID?: string`, `children: React.ReactNode`. It owns the overlay/scrim/slide/BackHandler/mount-unmount mechanics **and** the handle bar + header row (title + × close) + optional sub-line — i.e. everything the issue calls "chrome" except the grid, which is `children`.                             | (a) Shell owns only overlay/scrim/slide (no title/header), pushing header markup into every caller. (b) Promote to a brand-new top-level component directory (`src/components/AnimatedSheet/`) with its own styles/tests/stories. | Issue's own scope text is explicit: "reusable wrapper that takes visible/onClose/title (+ optional sub-line) and renders arbitrary body content" — header/handle/× must be shared, not re-implemented per caller (confirmed by the issue's instruction to fully delete `EditGoalView.styles.ts`'s `picker*` block, which includes `pickerHandle`/`pickerHeader`/`pickerTitle`/`pickerClose`/`pickerCloseIcon` — Edit Goal is left with no styles of its own to build a local header). A brand-new top-level directory adds a barrel/tests/stories surface the issue's "keep it near EvidenceTypePicker" option already avoids; two consumers doesn't yet justify promoting it. |
| D2  | `AnimatedSheet.tsx` is **not** added to `EvidenceTypePicker/index.ts`'s barrel. `EditGoalStepList.tsx` imports it directly: `import { AnimatedSheet } from "../EvidenceTypePicker/AnimatedSheet";`.                                                                                                                                                                                                                                                                                                      | Export via the barrel for a "proper" public API.                                                                                                                                                                                  | Mirrors the existing precedent: `CaptureSheetBody` is already imported directly (`from "./EvidenceTypePicker"`, bypassing the barrel) by the stories file. Keeps the barrel's public surface exactly as-is, in the spirit of D7 (no second public picker) even though `AnimatedSheet` isn't itself a picker.                                                                                                                                                                                                                                                                                                                                                                   |
| D3  | `CaptureSheetBody` (in `EvidenceTypePicker.tsx`) stays a fully self-contained static render — its own handle + header + sub-line + grid — rather than delegating its header/handle to `AnimatedSheet`. It is **not** rendered through `AnimatedSheet` when used standalone by the `AllThemesMatrix` story.                                                                                                                                                                                               | Have `CaptureSheetBody` delegate its header/handle to a shared static sub-component so there's exactly one implementation everywhere.                                                                                             | The issue explicitly requires the matrix story keep rendering `CaptureSheetBody` directly without a scrim (seven live scrims would stack over the canvas). Keeping `CaptureSheetBody`'s prop signature (`headerTitle`/`activeStepTitle`/`selectedType`/`onSelectType`/`onClose`) and rendered output byte-identical means **zero** risk to the story or its existing `EvidenceTypePicker.test.tsx` coverage. The trade-off is ~20 lines of handle/header/sub-line markup existing in two places (`AnimatedSheet` and `CaptureSheetBody`) — small, low-risk, and consistent with the issue's own acceptance that the matrix path is a deliberate special case.                  |
| D4  | Extract the single-select radio grid (`EVIDENCE_OPTIONS.map(...)`, currently inline in `CaptureSheetBody`, `EvidenceTypePicker.tsx:320-345`) into a small unexported `CaptureGrid({ selectedType, onSelectType })` helper in the same file. Both `CaptureSheetBody` (story path) and the live `CaptureSheet`→`AnimatedSheet` path render `<CaptureGrid />` — so the grid markup itself has exactly one source, even though D3 keeps the surrounding chrome markup duplicated.                            | Leave the grid inlined in `CaptureSheetBody` only, and have the live path call `<CaptureSheetBody />` as `children` of `AnimatedSheet` (double header).                                                                           | Calling `CaptureSheetBody` as `AnimatedSheet`'s children would render the header/handle _twice_ (once from `AnimatedSheet`, once from `CaptureSheetBody`). Extracting just the grid avoids that while keeping D3's isolation.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D5  | `AnimatedSheet`'s backdrop keeps the existing testID `"capture-sheet-backdrop"` unconditionally, used by both the capture path and the Edit Goal path.                                                                                                                                                                                                                                                                                                                                                   | Add a configurable `backdropTestID` prop.                                                                                                                                                                                         | `EvidenceTypePicker.test.tsx:279` and `NewGoalWizard.test.tsx:306,504` already assert this literal string; Edit Goal has no existing test on the backdrop's testID (verified by grep — only `edit-goal-evidence-close` is asserted), and the two sheets are never mounted simultaneously, so one shared literal is simplest and keeps both existing suites green with zero prop-surface growth.                                                                                                                                                                                                                                                                                |
| D6  | `AnimatedSheet`'s × close `Pressable` accepts an optional `closeTestID` prop (undefined → no testID, matching today's capture-mode close button which has none). `EditGoalStepList.tsx` passes `closeTestID="edit-goal-evidence-close"`.                                                                                                                                                                                                                                                                 | Hardcode one testID for the close button too.                                                                                                                                                                                     | `EditGoalView.test.tsx` asserts `"edit-goal-evidence-close"` in 3 places (lines 282, 284, 421); capture-mode tests find the close button by text (`"✕"`), not testID. A configurable prop preserves both without touching either test file.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D7  | `AnimatedSheet` takes plain, already-resolved strings (`title`, `subLine`, `closeLabel`) and has **no** `react-i18next` dependency itself. Default-text resolution (`"Add evidence"`, the `savingToActiveStep` interpolation, `"Close"`) stays in `CaptureSheet` (which already calls `useTranslation`); `EditGoalStepList` already threads pre-resolved copy props (`evidencePickerTitle`, `closeLabel`) per its own file-header note ("Copy is i18n-free per D9 [of #489]; \[Integrate\] passes t()"). | Give `AnimatedSheet` its own `useTranslation` call with its own default keys.                                                                                                                                                     | Keeps the new shared primitive decoupled from i18n plumbing (it's UI-generic), and matches the calling convention Edit Goal already uses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Affected Areas

- `src/components/EvidenceTypePicker/AnimatedSheet.tsx` (new): the shared animated shell — overlay, scrim, backdrop, slide transform, Android `BackHandler`, mount/rendered-while-animating-out lifecycle (verbatim mechanics lifted from today's `CaptureSheet`, `EvidenceTypePicker.tsx:176-253`), plus handle + header (title + ×) + optional sub-line. Reuses existing style keys from `EvidenceTypePicker.styles.ts` (`overlay`, `scrim`, `backdrop`, `sheet`, `handle`, `sheetHeader`, `sheetTitle`, `closeButton`, `closeIcon`, `subLine`) — no new styles file.
- `src/components/EvidenceTypePicker/EvidenceTypePicker.tsx`: extract `CaptureGrid` (D4); rewire the internal `CaptureSheet` function (capture mode's live path) to render `<AnimatedSheet>` wrapping `<CaptureGrid>` instead of its own overlay/scrim/`View`/`Animated.View` markup. `CaptureSheetBody`'s exported signature and rendered output are unchanged (D3) other than internally calling `<CaptureGrid>` instead of inlining the map.
- `src/components/EditGoalView/EditGoalStepList.tsx`: delete the `Modal`+`SafeAreaView` block (`:460-501`) and its `pickerOverlay`/`pickerBackdrop`/`pickerSheet`/`pickerHandle`/`pickerHeader`/`pickerTitle`/`pickerClose`/`pickerCloseIcon` style references; render `<AnimatedSheet>` wrapping the existing multi-select `<EvidenceTypePicker selectedTypes={...} onToggleType={handleToggleEvidence} label={evidenceTypesLabel} />`. Drop the now-unused `Modal` (from `react-native`) and `SafeAreaView` (from `react-native-safe-area-context`) imports — both are used nowhere else in this file.
- `src/components/EditGoalView/EditGoalView.styles.ts`: delete the dead `picker*` block (`:379-434` — 8 keys: `pickerOverlay`, `pickerBackdrop`, `pickerSheet`, `pickerHandle`, `pickerHeader`, `pickerTitle`, `pickerClose`, `pickerCloseIcon`).
- `src/components/EditGoalView/__tests__/EditGoalView.test.tsx`: add coverage for behavior the old `Modal` never had (Android back dismiss) — see Testing Strategy.
- **Unchanged** (verified, no edits needed): `EvidenceTypePicker/index.ts` barrel (D2/D7), `EvidenceTypePicker.styles.ts` (AnimatedSheet reuses its existing keys as-is), `EvidenceTypePicker.stories.tsx` (D3 keeps `CaptureSheetBody`'s call signature identical), `NewGoalWizard.tsx` (still calls `<EvidenceTypePicker mode="capture" .../>` exactly as today — it inherits `AnimatedSheet` transitively with no API change on its side).

## Implementation Plan

### Step 1: Extract `AnimatedSheet` and rewire the capture-mode sheet onto it

**Files**: `src/components/EvidenceTypePicker/AnimatedSheet.tsx` (new), `src/components/EvidenceTypePicker/EvidenceTypePicker.tsx`
**Commit**: `refactor(evidence-picker): extract AnimatedSheet shell from CaptureSheet`
**Changes**:

- [ ] Create `AnimatedSheet.tsx` exporting `AnimatedSheet` + `AnimatedSheetProps` (D1). Lift the `progress`/`rendered`/`useEffect` timing/`BackHandler` mechanics verbatim from today's `CaptureSheet` (`EvidenceTypePicker.tsx:176-253`). Render: overlay `View` → scrim `Animated.View` with the backdrop `Pressable` (testID `"capture-sheet-backdrop"`, D5, `accessibilityLabel={closeLabel}`) → sheet `Animated.View` (slide transform) containing: handle, header row (title `Text accessibilityRole="header"` + × close `Pressable` with `accessibilityLabel={closeLabel}`, optional `testID={closeTestID}`, D6), optional `subLine` `Text`, then `{children}`.
- [ ] In `EvidenceTypePicker.tsx`, extract `CaptureGrid({ selectedType, onSelectType })` (D4) from `CaptureSheetBody`'s current grid block (`:320-345`) — same `EVIDENCE_OPTIONS.map`, same `effectiveSelected = selectedType ?? EvidenceType.text` default (D5 of #463, unchanged), same accessibility props.
- [ ] Rewire the internal `CaptureSheet` function to: resolve `title = headerTitle ?? t("common:evidenceTypePicker.addEvidence")`, `subLine = activeStepTitle ? t("common:evidenceTypePicker.savingToActiveStep", { title: activeStepTitle }) : undefined`, `closeLabel = t("common:actions.close")`, then render `<AnimatedSheet visible={visible} onClose={onClose} title={title} subLine={subLine} closeLabel={closeLabel}><CaptureGrid selectedType={selectedType} onSelectType={onSelectType} /></AnimatedSheet>`.
- [ ] Update `CaptureSheetBody` to call `<CaptureGrid selectedType={selectedType} onSelectType={onSelectType} />` in place of the inlined map; leave its handle/header/sub-line/box markup and prop signature untouched (D3).
- [ ] Update `CaptureSheetBody`'s doc comment to note it deliberately duplicates `AnimatedSheet`'s chrome markup so it can render standalone for the theme matrix (D3), and that the grid itself is shared via `CaptureGrid` (D4).
- [ ] Run `bun run test --testPathPatterns EvidenceTypePicker` — every existing capture-mode + full-mode test must pass unmodified (this is the regression gate proving the rewire produced identical output).
- [ ] Run `bun run test --testPathPatterns NewGoalWizard` — the wizard's `capture-sheet-backdrop` assertions (lines 306, 504) must keep passing.
- [ ] Run `bun run type-check` and `bun run lint`.

### Step 2: Retire Edit Goal's `Modal` and delete the dead styles

**Files**: `src/components/EditGoalView/EditGoalStepList.tsx`, `src/components/EditGoalView/EditGoalView.styles.ts`, `src/components/EditGoalView/__tests__/EditGoalView.test.tsx`
**Commit**: `refactor(edit-goal): replace picker Modal with shared AnimatedSheet`
**Changes**:

- [ ] Import `AnimatedSheet` directly from `"../EvidenceTypePicker/AnimatedSheet"` (D2).
- [ ] Replace the `Modal`/`SafeAreaView` block (`:460-501`) with `<AnimatedSheet visible={editingEvidenceTypes !== undefined} onClose={() => setEditingEvidenceId(null)} title={evidencePickerTitle} closeLabel={closeLabel} closeTestID="edit-goal-evidence-close">{editingEvidenceTypes ? <EvidenceTypePicker selectedTypes={editingEvidenceTypes} onToggleType={handleToggleEvidence} label={evidenceTypesLabel} /> : null}</AnimatedSheet>`. No `subLine` — Edit Goal has never had one.
- [ ] Remove the now-unused `Modal` import (from `react-native`) and `SafeAreaView` import (from `react-native-safe-area-context`) — confirmed unused elsewhere in this file.
- [ ] Delete the `picker*` block from `EditGoalView.styles.ts` (`:379-434`, 8 keys).
- [ ] Add to `EditGoalView.test.tsx`, in the existing `describe("evidence picker (D8)")` block: a test that opens the picker (`fireEvent.press(screen.getByTestId("edit-goal-step-evidence-s1"))`), spies on `BackHandler.addEventListener` (mirroring `EvidenceTypePicker.test.tsx`'s "calls onClose on Android hardware back" pattern), invokes the captured `"hardwareBackPress"` handler, and asserts it returns `true` and the picker closes (`screen.queryByTestId("edit-goal-evidence-close")` becomes `null`).
- [ ] Confirm (no new test needed — already covered) the existing "opens the multi-select picker...", "adds a type on toggle...", and "refuses to deselect the last remaining type..." tests still pass unmodified — they exercise the exact selection semantics D1-D7 must not touch.
- [ ] Run `bun run test --testPathPatterns EditGoalView`, `bun run type-check`, `bun run lint`.

## Testing Strategy

- [ ] `bun run test --testPathPatterns EvidenceTypePicker` stays 100% green with zero test-file edits (regression gate for Step 1's rewire — see Step 1 checklist).
- [ ] `bun run test --testPathPatterns NewGoalWizard` stays green (capture-sheet-backdrop assertions, D5).
- [ ] `bun run test --testPathPatterns EditGoalView` stays green plus the new Android-back test added in Step 2.
- [ ] Manual/Storybook verification (per the issue's "Verify" section): open `EditGoalView`'s default story, tap a step's evidence chip, confirm the reanimated slide-up + scrim fade (not the OS `Modal` slide) and that Android hardware back (device/emulator) dismisses it. Toggle the animation-preference/Autism-Friendly theme and confirm the same sheet appears/dismisses instantly. Re-check `EvidenceTypePicker.stories.tsx`'s `CaptureSheet`, `CaptureSheetChangeScenario`, `AllThemesMatrix`, and `AuthoringAllThemes` stories render unchanged.
- [ ] `bun run type-check` and `bun run lint` clean after each commit.

## Not in Scope

| Item                                                                                                                                                                                                       | Reason                                                                                                                                                                                                         | Follow-up |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| Fixing that Edit Goal's grid content unmounts instantly (before the shell finishes its exit slide/fade) because `editingEvidenceTypes` flips to `undefined` in the same state update that closes the sheet | Pre-existing in today's `Modal`-based code (same instant-unmount-of-children-prop pattern) — not a regression introduced by this refactor, and the issue's "no visual redesign" instruction doesn't ask for it | none      |
| Fully unifying `CaptureSheetBody`'s standalone markup with `AnimatedSheet`'s markup to remove the D3 duplication                                                                                           | Would require rewriting `EvidenceTypePicker.stories.tsx`'s `AllThemesMatrix` for no functional gain; the issue explicitly protects that story's current shape                                                  | none      |
| Promoting `AnimatedSheet` to a top-level `src/components/` directory with its own barrel/tests/stories                                                                                                     | Two internal consumers (both already inside `EvidenceTypePicker`'s "neighborhood") don't yet justify a new public component surface (D1)                                                                       | none      |
| Any change to the New Goal wizard's own capture-sheet call site                                                                                                                                            | It already uses `mode="capture"` and inherits `AnimatedSheet` transitively with zero API change on its side                                                                                                    | none      |

## Discovery Log

<!-- Entries added by implement skill:
- [YYYY-MM-DD HH:MM] <discovery description>
-->
