# Development Plan: Issue #463

## Issue Summary

**Title**: [Storybook] New Goal wizard 2/3 — step 2 · first step + planned-evidence chip
**Type**: feature (Storybook-only slice)
**Complexity**: SMALL
**Estimated Lines**: ~350-420 lines across 7 files (wizard component/styles/stories/tests + EvidenceTypePicker prop extension, its test, en/common.json)

## Intent Verification

- [ ] When a reviewer opens the `Iteration B/Goals/NewGoalWizard` Storybook entry and selects `Step2`, they see: "Goal" eyebrow + the goal title recap, headline "What's the first step?", a text input placeholder "One small thing to start", and a purple planned-evidence pill reading "📝 Note" with a "change" link beside it — all before typing anything (the "born with a planned evidence type" invariant, never an empty/"no type chosen" state).
- [ ] Tapping the pill (or "change") opens `EvidenceTypePicker`'s `mode="capture"` bottom sheet — same 6-type grid, header reading **"Evidence type"** via the new optional `headerTitle` prop (prototype-canonical copy; D3) — with no `activeStepTitle` sub-line (there is no active step yet during goal creation).
- [ ] Selecting a different type in the sheet updates the pill's icon/label immediately and closes the sheet; the `Step2PickerOpen` story shows the sheet pre-opened (a static, no-interaction visual state) for the same visual-gate reason `AllThemesMatrix`-style stories exist elsewhere.
- [ ] The step-2 "Next →" CTA is disabled while the first-step title is empty/whitespace-only (mirrors step 1's existing guard) and never fires `onNext` while disabled.
- [ ] `bun run test --testPathPatterns NewGoalWizard` and `bun run test --testPathPatterns EvidenceTypePicker` stay green; `bun run type-check` and `bun run lint` are clean; zero hardcoded hex in the diff (`theme.colors.*` / `theme.radius.*` / `theme.space.*` only).

## Dependencies

| Issue | Title                                                 | Status                                                       | Type                              |
| ----- | ----------------------------------------------------- | ------------------------------------------------------------ | --------------------------------- |
| #462  | [Storybook] New Goal wizard 1/3 — shell + steps 1 & 4 | ✅ Met (merged `f2e5b4e2`, on `main`)                        | Blocker                           |
| #409  | [Storybook] Evidence-type picker sheet                | ✅ Met (merged, `CaptureSheetBody`/`mode="capture"` shipped) | Blocker (implicit — "reuse #409") |
| #443  | New Goal wizard umbrella                              | 🟡 Open (umbrella; closes when #462–#464 close)              | Parent, non-blocking              |

**Status**: ✅ All dependencies met. `has_blockers = false`.

The issue's `dep:blocked` label and "Blocked by slice 1/3" text are **stale** — slice 1/3 (#462) merged to `main` at commit `f2e5b4e2` ("[Storybook] New Goal wizard 1/3 — shell + steps 1 & 4 (name, ready) (#462) (#473)"), present in this branch. `NewGoalWizard.tsx`, `.styles.ts`, `.stories.tsx`, and `__tests__/NewGoalWizard.test.tsx` all exist at `apps/native-rd/src/components/NewGoalWizard/`. The `dep:blocked` label should be removed from #463 once this plan lands (flagged for the implementer/finalize step, not auto-editable here).

## Objective

Fill the `currentStep === "step"` branch of the existing `NewGoalWizard` component (left as an inert placeholder by slice 1/3) with the App Shell prototype's step-2 body: a goal-title recap, the "first step" title input, and a planned-evidence pill that composes #409's `EvidenceTypePicker` (`mode="capture"`) as its change sheet — without forking it. Ships as Storybook-only (no screen/nav wiring; `[Integrate]` #444 owns that), matching slice 1/3's shape exactly.

## Decisions

| ID  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Alternatives Considered                                                                 | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Evidence-sheet visibility (`evidencePickerOpen`) is a **controlled prop** (`evidencePickerOpen` / `onOpenEvidencePicker` / `onCloseEvidencePicker`), not component-internal `useState`.                                                                                                                                                                                                                                                                                                                                                            | `EditGoalView` owns its analogous `editingEvidenceId` as internal state.                | `NewGoalWizard`'s own docblock declares it "Pure, prop-driven... no navigation, no persistence" (D5, slice 1/3) with zero internal state today (`currentStep` itself is external). The issue explicitly requires a `Step2PickerOpen` **story** — a controlled prop makes that a trivial `useState(true)` in the story wrapper (mirrors `EvidenceTypePicker.stories.tsx`'s own `CaptureSheetDemo`); internal state would require a `play()` function or a story-only escape-hatch prop, both worse.                                                                                                                                                                         |
| D2  | Compose `<EvidenceTypePicker mode="capture" .../>` **directly** (its own Modal, backdrop, header, grid) — do not hand-roll a sheet like `EditGoalView` did.                                                                                                                                                                                                                                                                                                                                                                                        | Copy `EditGoalView`'s custom Modal+`CaptureSheetBody` composition.                      | `EditGoalView` hand-rolled its sheet because it needs **multi-select authoring** mode (no built-in Modal wrapper exists for that mode). Step 2 is **single-select** (one planned type per step) — exactly what `mode="capture"` already models end-to-end. Using it whole is the literal reading of "reuse #409 as the sheet — don't fork it."                                                                                                                                                                                                                                                                                                                             |
| D3  | Sheet renders with **no `activeStepTitle`** — the "Saving to your active step · {title}" sub-line is omitted entirely (prop is optional; component already hides the line when absent). Header copy: add an **optional `headerTitle` prop** to `EvidenceTypePicker` capture mode (default = current `t("common:evidenceTypePicker.addEvidence")` → "Add evidence"; all existing callers untouched). The wizard passes the prototype's **"Evidence type"** via a new `common:evidenceTypePicker.evidenceType` en key. **User decision 2026-07-06.** | Keep "Add evidence" unmodified (researcher's original resolution).                      | There is no "active step" during goal creation, so the sub-line is semantically wrong regardless of copy. On the header: the prototype is canonical and "Evidence type" is the honest verb during goal creation (choosing a _planned_ type, not capturing evidence now). An optional, backward-compatible prop is an **extension, not a fork** — "don't fork" forbids copying the component, and this keeps one source of truth while restoring prototype fidelity. Escalated to the user; user chose the prop.                                                                                                                                                            |
| D4  | Planned-evidence type defaults to `EvidenceType.text` ("Note") via a default parameter on `plannedEvidenceType`, never `undefined`/empty.                                                                                                                                                                                                                                                                                                                                                                                                          | Allow `plannedEvidenceType` to be optional/unset until the user opens the picker.       | Direct implementation of "every step is born with a planned evidence type" (#443, #409 D5) and the "never show missing/needed framing" invariant — the pill must render a real type on first paint, never an empty state.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D5  | New copy (`stepGoalEyebrow`, `stepHeadline`, `firstStepPlaceholder`, `evidenceEyebrow`, `changeEvidenceLabel`, `changeEvidenceAccessibilityLabel`, `plannedEvidenceLabel`) ships as **props with English-literal defaults**, no `useTranslation`/i18n keys added to `NewGoalWizard`.                                                                                                                                                                                                                                                               | Add an `en/newGoal.json` `step` block + register YAML, call `t()` inside the component. | Slice 1/3's own docblock (D5) established `NewGoalWizard` as **i18n-free by design** — `[Integrate]` (#444) threads real `t()` output through the props later. `en/newGoal.json` today only serves the unrelated legacy `NewGoalModal` screen. Adding i18n here would contradict the established, already-shipped pattern for this exact component. `plannedEvidenceLabel` is a function-prop (default: plain map mirroring `common:evidenceTypes.*.label` strings exactly — Photo/Video/Audio/Note/Link/File) mirroring the existing `stepCountSummary` prop shape, so #444's swap-in is a pure plumbing change with no copy drift.                                       |
| D6  | Step-2 "Next →" is **disabled** while `firstStepTitle` is empty/whitespace, reusing the existing `onNext` prop (no new callback).                                                                                                                                                                                                                                                                                                                                                                                                                  | Leave Next always-enabled (prototype shows no disabled state for this screen).          | Symmetry with step 1's already-shipped guard on the same control in the same component; the prototype is a static mock and doesn't model interaction-disabled states at all (it doesn't model step 1's disabled Next either, which slice 1/3 already added deliberately). Reusing `onNext` matches the prototype's own state machine, where one `next` handler advances `name → step → build → ready` linearly.                                                                                                                                                                                                                                                            |
| D7  | Planned-evidence pill is **one collapsed-`accessible` `Pressable`** (icon + label + "change" all one a11y node), not two separate tap targets as the prototype's raw HTML models (two adjacent `onClick` spans).                                                                                                                                                                                                                                                                                                                                   | Mirror the prototype's two separate `openFirstEv` spans as two Pressables.              | Matches the established, already-shipped `FocusCurrentTaskCard` "planned-evidence box" pattern exactly (`plannedBox` Pressable wrapping icon+label+"change" as one accessible node, with a descriptive `accessibilityLabel` naming the action and current type) — the closest real precedent for this exact UI shape in this codebase.                                                                                                                                                                                                                                                                                                                                     |
| D8  | Pill visual treatment: fully-rounded shape (`theme.radius.full` — the adapter maps the package's `pill` value to `radius.full`, `adapter.ts:101`), `theme.colors.accentPurpleLight` background (not `FocusCurrentTaskCard`'s bordered-card `plannedBox`), `shadowStyle(theme, "hardSm")` (the small hard-shadow token; theme-aware so a11y themes that disable shadows drop it — final elevation is a Storybook visual-gate call against the prototype), DM Mono ~12px label.                                                                      | Reuse `FocusCurrentTaskCard.plannedBox` styling verbatim.                               | Prototype's step-2 chip (`#ede9fe` bg, `border-radius:9999px`, 12px DM Mono) matches the **build-step row chip** and the **ready-step badge banner** in the _same_ prototype route (`accentPurpleLight`, already the token this exact file's own style comments map `#ede9fe` to) — not `FocusCurrentTaskCard`'s bigger bordered box, which is a different visual tier (Focus Mode's in-progress card, not the wizard's compact recap chip). **Correction (verified 2026-07-06):** the researcher's original `theme.radius.pill` and `shadowStyle(theme, "cardElevationSmall")` are not real tokens — only `radius.full` and shadow keys `hardSm`/`hardMd`/`hardLg` exist. |

## Affected Areas

- `apps/native-rd/src/components/NewGoalWizard/NewGoalWizard.tsx`: fill the `currentStep === "step"` branch (goal recap, first-step input, planned-evidence pill, composed `EvidenceTypePicker` capture sheet, updated docblock/`STEP_ORDER` comments now that #463 lands).
- `apps/native-rd/src/components/NewGoalWizard/NewGoalWizard.styles.ts`: add `stepGoalRecap`, evidence-row, pill (`evidenceChip`/`evidenceChipIcon`/`evidenceChipLabel`/`evidenceChipChange`) styles; update the `placeholderBody` comment (now only "build" is pending, #464).
- `apps/native-rd/src/components/NewGoalWizard/NewGoalWizard.stories.tsx`: add `Step2` (interactive, mirrors `InteractiveNameStep`) and `Step2PickerOpen` (same wrapper, `evidencePickerOpen` seeded `true`) stories, reusing the existing `PhoneStage` helper.
- `apps/native-rd/src/components/NewGoalWizard/__tests__/NewGoalWizard.test.tsx`: new `describe("step 2 · first step")` block; extend the existing cross-step `it.each` tests (onClose, headline `accessibilityRole="header"`, back-arrow) to include `"step"`; split the combined `it.each(["step", "build"])("renders the %s placeholder...")` test down to `["build"]` only (step 2 is no longer a placeholder).
- `apps/native-rd/src/components/EvidenceTypePicker/EvidenceTypePicker.tsx`: add optional `headerTitle?: string` to `EvidenceTypePickerProps` (capture mode) and `CaptureSheetBodyProps`, defaulting to the existing `t("common:evidenceTypePicker.addEvidence")` — backward-compatible extension, no behavior change for existing callers (D3).
- `apps/native-rd/src/i18n/locales/en/common.json`: one new key, `evidenceTypePicker.evidenceType: "Evidence type"` (en only — de/pseudo are bot-synced). `NewGoalWizard` itself stays i18n-free (D5); this key belongs to `EvidenceTypePicker`, which already uses i18n.
- `apps/native-rd/src/components/EvidenceTypePicker/__tests__/*`: one test for the `headerTitle` override + default.

## Implementation Plan

### Step 0: EvidenceTypePicker — optional capture-sheet header title ✅ DONE (commit `53b4ada4`)

**Files**: `EvidenceTypePicker.tsx`, `__tests__/EvidenceTypePicker.test.tsx`
**Commit**: `feat(evidence-type-picker): optional headerTitle for the capture sheet`
**Changes**:

- [x] Added `headerTitle?: string` to `CaptureEvidenceTypePickerProps` and `CaptureSheetBodyProps`; renders `headerTitle ?? t("common:evidenceTypePicker.addEvidence")` in the sheet header. No behavior change for existing (Focus Mode) callers.
- [x] ~~Add en key `evidenceTypePicker.evidenceType`~~ **DEFERRED to #444** (plan's own contingency, taken). Rationale: `locale-parity.test.ts` fails on ANY en↔pseudo key drift, and it runs in-PR **before** the de/pseudo sync bot fires on `main` — so an en-only orphan key breaks CI immediately. The key is genuinely unused this slice: the wizard is i18n-free (D5) and carries the literal `"Evidence type"` default via `evidenceSheetTitle`; `EvidenceTypePicker.headerTitle` defaults to the existing `addEvidence` key. D3's user-chosen "Evidence type" copy still renders correctly via the literal prop default. #444 adds the key + threads `t()` when it does i18n integration. (Also: real locale path is `src/i18n/resources/en/`, not `locales/en/` — plan typo.)
- [x] Test: default header still "Add evidence"; override renders the passed title. (28 tests pass.)

### Step 1: Step-2 body — recap, input, planned-evidence pill, composed capture sheet ✅ DONE (commit `9cdba211`)

**Files**: `NewGoalWizard.tsx`, `NewGoalWizard.styles.ts`
**Commit**: `feat(new-goal-wizard): step 2 first step — goal recap, input, planned-evidence chip`
**Changes**:

- [x] Added props: `firstStepTitle`, `onFirstStepTitleChange`, `plannedEvidenceType = EvidenceType.text`, `onPlannedEvidenceTypeChange`, `evidencePickerOpen`, `onOpenEvidencePicker`, `onCloseEvidencePicker`, plus copy props per D5. **All step-2 props are OPTIONAL with defaults** (callbacks default to a shared `noop`, data props to sensible values, copy to English literals). This keeps the existing `NameStep`/`ReadyStep` stories and the test `makeProps` helper compiling **untouched** — a deliberate deviation from the "all props required, every caller passes all" pattern to keep the diff focused. `goalTitle` stays required (genuinely cross-step: name input + step-2 recap + ready summary).
- [x] Imported `EvidenceType` (`../../db`), `EVIDENCE_OPTIONS`/`EvidenceTypeValue` (`../../types/evidence`), `EvidenceTypePicker` (`../EvidenceTypePicker`).
- [x] Added `DEFAULT_PLANNED_EVIDENCE_LABEL` map + `defaultPlannedEvidenceLabel` fn. Also added `defaultChangeEvidenceAccessibilityLabel = (label) => \`Change evidence type, currently ${label}\``— **mirrors`focusMode:currentTask.inProgress.changeEvidenceTypeA11y` verbatim\*\* (the FocusCurrentTaskCard precedent, D7).
- [x] Rendered the `currentStep === "step"` branch. **Chip structure (refined from D8's raw prototype read):** the `Pressable` (testID `new-goal-evidence-chip`, `accessible`, one a11y node) wraps a purple chip `View` (icon + label, `accentPurpleLight`/`radius.full`/`hardSm`) **plus** the plain-blue "change" link — "change" sits OUTSIDE the purple pill, matching the prototype (line 369) where the pill and "change" are adjacent, not nested. Icon has `importantForAccessibility="no"`. Chip label/icon/change all `size.xs` (12, matches prototype).
- [x] Rendered `<EvidenceTypePicker mode="capture" visible={evidencePickerOpen} headerTitle={evidenceSheetTitle} selectedType={plannedEvidenceType} onSelectType={...} onClose={onCloseEvidencePicker} />` unconditionally at container bottom (D2/D3 — no `activeStepTitle`). `evidenceSheetTitle` defaults to literal "Evidence type" (D5); #444 threads `t()`.
- [x] Updated docblock + `STEP_ORDER` comment: step 2 filled, only "build" (#464) remains.
- [x] `testID`s: `new-goal-first-step-input`, `new-goal-evidence-chip`; reuses `new-goal-next-button`.

**Verified so far:** `bun run type-check` clean; existing 26 `NewGoalWizard` tests + 28 `EvidenceTypePicker` tests green; pre-commit eslint/prettier hooks pass on both commits.

### Step 2: Stories — Step2 and Step2PickerOpen

**Files**: `NewGoalWizard.stories.tsx`
**Commit**: `test(new-goal-wizard): step-2 story incl. picker-open state`
**Changes**:

- [ ] `InteractiveStep2` wrapper (mirrors `InteractiveNameStep`): owns `firstStepTitle`, `plannedEvidenceType`, `evidencePickerOpen` via `useState`; seeds `goalTitle="Build a birdhouse"` (matches slice 1/3's sample data) and `firstStepTitle=""` so the disabled-Next state is visible by default.
- [ ] `Step2: Story` — closed sheet, default state.
- [ ] `Step2PickerOpen: Story` — same wrapper with `evidencePickerOpen` seeded `true` on mount (static, no interaction needed to see the sheet — satisfies the issue's explicit "incl. picker-open state" story ask).

### Step 3: Unit tests — step-2 coverage + cross-step regression updates

**Files**: `__tests__/NewGoalWizard.test.tsx`
**Commit**: `test(new-goal-wizard): step-2 component coverage`
**Changes**:

- [ ] New `describe("step 2 · first step")`: renders goal recap + headline + input + default "📝 Note" pill + "change" text + Next button; fires `onFirstStepTitleChange`; Next disabled for empty/whitespace `firstStepTitle` (mirror the existing `it.each` pattern) and enabled otherwise; pressing the pill fires `onOpenEvidencePicker`; sheet content (header `"Evidence type"`) appears when `evidencePickerOpen`; selecting a type in the sheet fires `onPlannedEvidenceTypeChange` and `onCloseEvidencePicker`; default `plannedEvidenceType` (prop omitted) renders "Note"/📝 (D4 regression test).
- [ ] Extend `it.each(["name", "ready"])("fires onClose from the %s step", ...)` → `["name", "step", "ready"]`.
- [ ] Extend the headline `accessibilityRole="header"` `it.each` table with `["step", "What's the first step?"]`.
- [ ] Extend the back-arrow-present/fires assertions to cover `"step"` (currently only asserted for `"ready"`).
- [ ] Split `it.each(["step", "build"])("renders the %s placeholder without a body regression", ...)` → `it("renders the build placeholder without a body regression", ...)` (drop `"step"`; it now has real content covered by its own describe block above).

## Testing Strategy

- [ ] Unit tests for `NewGoalWizard` step 2 (Jest 30, `@testing-library/react-native` v13) — run via `bun run test --testPathPatterns NewGoalWizard`.
- [ ] Re-run `bun run test --testPathPatterns EvidenceTypePicker` to confirm composing it inline doesn't regress its own suite (no changes to that component, but sanity-check).
- [ ] `test.each` for the disabled/empty-title cases (mirrors the existing step-1 pattern) and the cross-step `it.each` tables.
- [ ] Manual/Storybook: `Iteration B/Goals/NewGoalWizard` → `Step2` (type in the input, tap the pill, pick a different type, confirm the pill updates and the sheet closes) and `Step2PickerOpen` (visual check of the composed sheet inside the wizard frame, all 7 product themes via the toolbar switcher per the ScopedTheme re-render caveat — no `AllThemesMatrix` story here, that's slice 3/3's job per #443).

## Not in Scope

| Item                                                                     | Reason                                                                                 | Follow-up                       |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | ------------------------------- |
| Screen/navigation/persistence wiring (real `t()`, Evolu writes, routing) | `NewGoalWizard` stays pure/prop-driven per D5 (slice 1/3); this is `[Integrate]`'s job | #444                            |
| `AllThemesMatrix` story for the wizard                                   | Explicitly deferred to slice 3/3 per #443's scope split                                | #464                            |
| Multi-step "build" list body                                             | Placeholder remains; owned by slice 3/3                                                | #464                            |
| Smaller-steps / one-level nesting tier on the build screen               | Resolved-as-defer at the umbrella level                                                | #465                            |
| `dep:blocked` label removal on #463                                      | Labeling is a GitHub-metadata housekeeping action, not a code change                   | none — flag at finalize/PR time |

## Discovery Log

- [2026-07-06] **Step 0 committed** `53b4ada4` — `headerTitle` prop on EvidenceTypePicker capture mode. 28 EvidenceTypePicker tests pass, type-check clean.
- [2026-07-06] **Step 1 committed** `9cdba211` — step-2 body + styles. 26 NewGoalWizard tests still pass (existing suite unmodified), type-check clean.
- [2026-07-06] **Deferred the `evidenceType` en key to #444.** `locale-parity.test.ts` (`src/i18n/__tests__/`) fails on any en↔pseudo key-set drift and runs in-PR, before the de/pseudo sync bot fires on `main`. An en-only pre-staged key would red the build. The key is unused this slice anyway (wizard is i18n-free; carries the literal "Evidence type" via `evidenceSheetTitle`). This is the plan's own Step-0 contingency ("if an unused-key check rejects it, move to #444"). No en/de/pseudo files touched.
- [2026-07-06] **Real locale path** is `src/i18n/resources/en/common.json` (plan said `locales/en/` — typo, no key added anyway).
- [2026-07-06] **Step-2 props made optional-with-defaults**, not required — keeps existing `NameStep`/`ReadyStep` stories + test `makeProps` compiling untouched. Callbacks default to a shared `noop`.
- [2026-07-06] **Chip a11y label mirrors** `focusMode:currentTask.inProgress.changeEvidenceTypeA11y` ("Change evidence type, currently {{type}}") — the FocusCurrentTaskCard precedent (D7).
- [2026-07-06] **Chip structure:** "change" link sits OUTSIDE the purple pill (adjacent, both inside one accessible `Pressable`), matching prototype line 369 — not nested inside the purple bg.

## Handoff — remaining work (context clear point, 2026-07-06)

**Branch:** `feat/issue-463-newgoal-step2-first-step`. **2 of 4 commits landed** (Steps 0 + 1). No PR opened yet. Nothing pushed.

**Remaining, in order:**

1. **Step 2 — stories** (`NewGoalWizard.stories.tsx`). Add `InteractiveStep2` wrapper (mirror `InteractiveNameStep`; owns `firstStepTitle`/`plannedEvidenceType`/`evidencePickerOpen` via `useState`; seed `goalTitle="Build a birdhouse"`, `firstStepTitle=""` so disabled-Next shows). Export `Step2` (sheet closed) and `Step2PickerOpen` (`evidencePickerOpen` seeded `true`). Reuse `PhoneStage`. Commit: `test(new-goal-wizard): step-2 story incl. picker-open state`.
2. **Step 3 — tests** (`__tests__/NewGoalWizard.test.tsx`). New `describe("step 2 · first step")` (see Step 3 checklist above for the full case list, incl. D4 default-"Note" regression). Extend cross-step `it.each`: add `"step"` to the onClose table; add `["step", "What's the first step?"]` to the headline header-role table; add `"step"` to back-arrow present/fires assertions. **Split** the `it.each(["step","build"])(...placeholder...)` down to `["build"]` only (step 2 now has real content — note: the existing combined test currently still passes for "step" by luck, but it must be split so "step" is covered by the real describe block, not the placeholder assertion). Commit: `test(new-goal-wizard): step-2 component coverage`.
3. **Final gates + push** (task #5). `bun run type-check`, `bun run lint`, `bun run test --testPathPatterns "NewGoalWizard|EvidenceTypePicker"`. Confirm zero hardcoded hex in the diff. Then push branch + open PR (per `.claude/rules/push-review-fixes.md` / `/finalize`). **Flag at PR time:** remove the stale `dep:blocked` label from #463 (see Dependencies note).

**Key invariants to preserve:** wizard stays i18n-free (D5); every step born with a planned evidence type (D4, never empty); `theme.colors.*`/`theme.radius.*`/`theme.space.*` only (zero hex); don't add the `evidenceType` en key here (see Discovery Log).
