# Development Plan: Issue #464

## Issue Summary

**Title**: [Storybook] New Goal wizard 3/3 — step 3 · build list + flow story + AllThemesMatrix
**Type**: feature (Storybook-only slice)
**Complexity**: MEDIUM (borderline — see sizing note below)
**Estimated Lines**: ~460-560 lines across the same 4 files slices 1/3 & 2/3 touched (component, styles, stories, tests) — at or slightly over the issue's own ~400-500 LOC estimate; flagged, not split (see Not in Scope / sizing note).

## Intent Verification

- [x] When a reviewer opens `Iteration B/Goals/NewGoalWizard` → `BuildStep`, they see: "Your steps" header + a live count, a card per step (number, title, purple evidence chip with icon+label), a "+ add another step" affordance below the list, and an "I'm ready →" primary CTA in the footer — matching the App Shell prototype's `ng.isBuild` route exactly. _(code + test-verified; icon+label chip per plan/intent vs. prototype's icon-only — see Discovery Log. Storybook visual pass pending.)_
- [x] Tapping "+ add another step" appends a new row with the default title "New step" and the default "📝 Note" evidence chip (never an empty/unset chip — the "every step is born with a planned evidence type" invariant extends to added rows too) and the visible step count increments. _(append/default lives in the story wrappers `appendBuildStep`; component fires `onAddStep`, test-covered.)_
- [x] Tapping any row's evidence chip opens the same composed `EvidenceTypePicker` capture sheet already used by step 2 (no second sheet instance, no fork); picking a type updates only that row's chip and closes the sheet. _(single shared picker derived by `currentStep`, D1; test-covered incl. pre-selection.)_
- [x] `InteractiveFlow` story lets a reviewer click through all four steps in order (name → first step → build → ready) with real state: the goal title and first-step title/evidence typed in earlier steps reappear correctly on the build list (row 1) and the ready summary card. _(story shipped; live click-through is the pending Storybook visual pass.)_
- [x] `AllThemesMatrix` story documents (and the toolbar theme switcher genuinely renders) all 7 product themes (`themeNames` from `themes/compose.ts`) with zero hardcoded hex in the diff. _(story shipped, zero-hex confirmed in diff; toolbar-switch across 7 themes is the pending manual visual pass.)_
- [x] `bun run test --testPathPatterns NewGoalWizard` stays green, including a regression test that the old build-step "inert placeholder" assertion is replaced by real-content assertions (no silent loss of coverage). _(58 pass; full suite 9782 pass. Placeholder test → `describe("build step")` real-content assertions.)_

## Dependencies

| Issue | Title                                                                         | Status                                                                 | Type                              |
| ----- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------- |
| #462  | [Storybook] New Goal wizard 1/3 — shell + steps 1 & 4                         | ✅ Met — merged `f2e5b4e2` / PR #473, on `main`                        | Blocker                           |
| #463  | [Storybook] New Goal wizard 2/3 — step 2 · first step + planned-evidence chip | ✅ Met — merged PR #480 (`25a0a41e`), issue closed, on `main`          | Blocker                           |
| #409  | [Storybook] Evidence-type picker sheet                                        | ✅ Met — merged PR #439, `EvidenceTypePicker`/`mode="capture"` shipped | Blocker (implicit — "reuse #409") |
| #443  | New Goal wizard umbrella                                                      | 🟡 Open — closes when #462–#464 close                                  | Parent, non-blocking              |

**Status**: ✅ All dependencies met. `has_blockers = false`.

The issue's `dep:blocked` label and "Blocked by slices 1/3 and 2/3" text are **stale**. Both slices are merged and present on this branch (verified: `apps/native-rd/src/components/NewGoalWizard/{NewGoalWizard.tsx,NewGoalWizard.styles.ts,NewGoalWizard.stories.tsx,__tests__/NewGoalWizard.test.tsx}` all exist and already implement the "name", "step", and "ready" bodies; only "build" remains an inert `placeholderBody` — confirmed by reading the component and its test `"renders the build placeholder without a body regression"`). Flag `dep:blocked` for removal at finalize/PR time (label housekeeping, not a code change).

## Objective

Fill the `currentStep === "build"` branch of the existing `NewGoalWizard` component with the App Shell prototype's `ng.isBuild` body (flat "Your steps" list, per-row evidence chip, add-step affordance, step count, "I'm ready →" CTA), add an interactive 4-step flow story that exercises the whole wizard end-to-end, and add an `AllThemesMatrix` story covering the 7 product themes — completing slice 3/3 and closing umbrella #443. Stays Storybook-only (no navigation/persistence; `[Integrate]` #444 owns that), matching slices 1/3 and 2/3's shape exactly.

## Decisions

| ID  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Alternatives Considered                                                                                                                              | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Per-row evidence-sheet targeting stays a **controlled prop pair**: `openBuildStepEvidenceId?: string \| null`, `onOpenBuildStepEvidence: (id: string) => void`, `onCloseBuildStepEvidence: () => void`. The single `<EvidenceTypePicker>` instance already rendered by the component (D2 of #463) is reused for both step 2's chip and step 3's rows — its `visible`/`selectedType`/`onSelectType` are derived from `currentStep` (`"step"` → the existing `evidencePickerOpen`/`plannedEvidenceType` wiring; `"build"` → `openBuildStepEvidenceId`/the matching `buildSteps` entry). | Internal `useState` keyed by row id, mirroring `EditGoalView`'s `editingEvidenceId`.                                                                 | `NewGoalWizard`'s own docblock (D1, #463) established a zero-internal-state, fully-controlled contract specifically so Storybook can seed static "picker open" states without a `play()` function. Extending that same pattern to N rows (vs. importing `EditGoalView`'s internal-state idiom) keeps this file internally consistent across all three slices rather than mixing two different state philosophies in one component.                                                                                                                                                      |
| D2  | `buildSteps: BuildStep[]` (`{ id: string; title: string; evidenceType: EvidenceTypeValue }`) is a **new, independent prop array** — not a refactor of the already-shipped `firstStepTitle`/`plannedEvidenceType` step-2 props into a unified `steps` list (even though the prototype's own state treats them as the same array, `ng.steps[0]`).                                                                                                                                                                                                                                       | Unify into one `steps` prop consumed by both step 2 (edits index 0) and step 3 (renders the full list), matching the prototype's literal data model. | Unifying would reopen and change the shape of #463's already-merged, already-tested public API in the same PR that's supposed to be an additive slice — risk and scope beyond "step 3 build list". The seam is bridged only at the _story_ level (the `InteractiveFlow` story seeds `buildSteps[0]` from `firstStepTitle`/`plannedEvidenceType` when advancing step→build), exactly the kind of cross-step reconciliation `[Integrate]` (#444) will do for real against Evolu data.                                                                                                     |
| D3  | `onAddStep: () => void` — **no arguments**. The caller (story now, `[Integrate]` later) decides the new row's default title (`"New step"`) and default evidence type (`EvidenceType.text` / "Note"). No inline title-entry input ships on the build screen.                                                                                                                                                                                                                                                                                                                           | Add a controlled `newStepTitle`/`onNewStepTitleChange` text input + button (typical "add item" UX).                                                  | Verified against the canonical prototype (`App Shell.dc.html` line 382, `ng.addStep`): tapping "+ add another step" immediately appends `{ t: "New step", ev: { icon: "📝", label: "Note" } }` with zero intermediate input state. The issue's own scope line ("add-step affordance (`+ add another step`)") names only the tap affordance, not a text field. Renaming the row isn't offered anywhere in this route either — matches "keep the build list flat" (no elaboration beyond the prototype).                                                                                  |
| D4  | Build-row title is **not editable** in this slice — static text, no `TextInput`, no rename affordance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Add a per-row rename input/pencil icon (`EditGoalStepRow` has one).                                                                                  | Same prototype-fidelity reasoning as D3 — the `isBuild` route renders `step.t` as plain text with no edit affordance; renaming steps is `EditGoalView`'s job (post-creation), not the creation wizard's. Keeping this out matches "must not do: keep the build list flat" (no extra tier of interaction beyond what's canonical).                                                                                                                                                                                                                                                       |
| D5  | Build rows carry **no journey-state coloring** (no pending/in-progress/paused/completed badge).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Reuse `journey-*` tokens / `TimelineNode`-style state coloring on each row.                                                                          | Verified: `journey-*` tokens (`themes/compose.ts` `Journey`, wired through `adapter.ts`) model **step state** (pending → in-progress → paused → completed) for a goal that has been started. Every row in the build list is pre-creation ("planned", the goal hasn't started — "Start Working" is still ahead of it), so there is no step state to render. This is not an open question; the token's own scope in the codebase answers it.                                                                                                                                              |
| D6  | `AllThemesMatrix` uses the **toolbar-switcher pattern** (review via the Storybook theme toolbar, one static render, explanatory note) — the `EditGoalView`/`TimelineStep`/`BadgesWall` precedent — not a live 7-cell `<ScopedTheme>` grid.                                                                                                                                                                                                                                                                                                                                            | Per-cell `<ScopedTheme name={name}>` matrix, the idiom used by `TimelineNode`/`FinishLine`/`FocusProgressStrip`.                                     | Verified: `EvidenceTypePicker.tsx` (rendered unconditionally by `NewGoalWizard` since #463) uses `useAnimationPref` + two `useEffect`s with `setState` — an async post-mount re-render. On web, `<ScopedTheme>` only themes the initial render pass; a later re-render reverts a per-cell wrapper to the active toolbar theme (the exact "null matrix" trap `EditGoalView`'s own `AllThemesMatrix` comment documents). `NewGoalWizard` inherits this trap transitively through the picker it already composes, so it needs the same toolbar-switcher treatment, not the per-cell idiom. |
| D7  | Build-step CTA reuses the existing `onNext` callback (same linear-advance semantics `STEP_ORDER` already models) with a **new copy prop** `buildReadyLabel = "I'm ready →"`, distinct from `nextLabel`'s `"Next →"` default used on name/step.                                                                                                                                                                                                                                                                                                                                        | Add a separate `onReady` callback.                                                                                                                   | The prototype's own state machine (`ngNext` map: `name→step→build→ready`) uses one `next` handler for every forward transition — only the _label_ changes per step (`"Next →"` vs `"I'm ready →"`). Matches the existing `nextLabel` copy-prop shape exactly; no new callback needed.                                                                                                                                                                                                                                                                                                   |
| D8  | The build header's step count is **derived internally** as `buildSteps.length` — not the existing top-level `stepCount` prop (which the "ready" step already consumes independently per slice 1/3's decoupled design, and which callers may compute differently before `[Integrate]` unifies the data model).                                                                                                                                                                                                                                                                         | Thread the existing `stepCount` prop into the build header too.                                                                                      | Avoids reconciling two potentially-divergent counts inside one component this slice; `stepCount` and `buildSteps` remain two independently-owned inputs until `[Integrate]` (#444) unifies them against real data, consistent with D2.                                                                                                                                                                                                                                                                                                                                                  |

## Affected Areas

- `apps/native-rd/src/components/NewGoalWizard/NewGoalWizard.tsx`: fill the `currentStep === "build"` branch (header + count, row list, add-step affordance, footer CTA); add `buildSteps`, `onAddStep`, `openBuildStepEvidenceId`, `onOpenBuildStepEvidence`, `onCloseBuildStepEvidence`, `buildReadyLabel`, and row-copy props (`yourStepsLabel`, `addStepLabel`, `addStepAccessibilityLabel`, a row-chip a11y-label fn); derive the shared `EvidenceTypePicker`'s `visible`/`selectedType`/`onSelectType` by `currentStep` (D1); remove the now-dead `placeholderBody` branch/style comment; update the docblock now that all 4 steps are filled.
- `apps/native-rd/src/components/NewGoalWizard/NewGoalWizard.styles.ts`: add build-header row style, a `ScrollView`-friendly top-aligned body style (build can't reuse the centered `stepBody`), the step-card style (reusing the `hardSm`/`accentPurpleLight` chip idiom already established for the evidence pill), and the add-step affordance row style (mirrors `quickAddPress`).
- `apps/native-rd/src/components/NewGoalWizard/NewGoalWizard.stories.tsx`: add `InteractiveBuildStep` wrapper + `BuildStep` story (seeded with the prototype's own sample data — "Sand the edges"/Note, "Paint it"/Photo, matching `initNG()`); add `InteractiveFlow` (owns `currentStep`, `goalTitle`, `firstStepTitle`/`plannedEvidenceType`, `buildSteps`, and wires `onNext`/`onQuickAdd`/`onBack`/`onStartWorking` to advance `name→step→build→ready`, seeding `buildSteps[0]` from the first-step data on the step→build transition per D2); add `AllThemesMatrix` (toolbar-switcher pattern, D6) with `themeNames`/`MOOD_NAMES` from `themes/compose.ts`.
- `apps/native-rd/src/components/NewGoalWizard/__tests__/NewGoalWizard.test.tsx`: new `describe("build step")` block; replace the existing `"renders the build placeholder without a body regression"` test (it currently asserts the _absence_ of body content) with real-content assertions; extend the cross-step `it.each` tables (`onClose`, back-arrow present/fires) to include `"build"` where not already covered (build already appears in the progress-segment and header-role tables — those stay unchanged).

## Implementation Plan

### Step 1: Build-step body — rows, add-step, evidence chip, footer CTA

**Files**: `NewGoalWizard.tsx`, `NewGoalWizard.styles.ts`
**Commit**: `feat(new-goal-wizard): step 3 build list — rows, add-step, evidence chip`
**Changes**:

- [x] Add `BuildStep` type export (`{ id: string; title: string; evidenceType: EvidenceTypeValue }`) and `buildSteps: BuildStep[]` (required — the build screen has nothing meaningful to render without it, unlike step 2's optional-with-defaults props).
- [x] Add `onAddStep: () => void` (D3, no args).
- [x] Add `openBuildStepEvidenceId?: string | null`, `onOpenBuildStepEvidence: (id: string) => void`, `onCloseBuildStepEvidence: () => void` (D1).
- [x] Add copy props with English defaults: `yourStepsLabel = "Your steps"`, `addStepLabel = "add another step"`, `addStepAccessibilityLabel = "Add another step"`, `buildReadyLabel = "I'm ready →"`, a row-chip a11y-label fn reusing `changeEvidenceAccessibilityLabel`'s shape (parameterized by row title + type, e.g. `buildStepEvidenceAccessibilityLabel(title, label)`).
- [x] Render the `currentStep === "build"` branch: header row (`yourStepsLabel` + `buildSteps.length`, D8), a `ScrollView` of row cards (number, title, evidence chip `Pressable` — same icon/label styling as the step-2 chip, sourced via `EVIDENCE_OPTIONS`/`DEFAULT_PLANNED_EVIDENCE_LABEL`, reused not duplicated), the "+ add another step" `Pressable` (mirrors `quickAddPress`'s single-accessible-node pattern), footer `Button` labeled `buildReadyLabel` calling `onNext` (D7, always enabled — the prototype gates nothing here and `buildSteps` can never be empty by the time this screen is reached).
- [x] Derive the single `<EvidenceTypePicker>`'s `visible`/`selectedType`/`onSelectType`/`onClose` by `currentStep` (D1): `"step"` keeps the existing `evidencePickerOpen`/`plannedEvidenceType` wiring untouched; `"build"` uses `openBuildStepEvidenceId != null` / the matching `buildSteps` entry's `evidenceType` / an `onSelectType` that updates that row via a new `onBuildStepEvidenceTypeChange(id, type)` callback and calls `onCloseBuildStepEvidence()`.
- [x] Add `onBuildStepEvidenceTypeChange: (id: string, type: EvidenceTypeValue) => void`.
- [x] Remove the dead `placeholderBody` branch and its style/comment (all 4 steps now render real content); update the top-of-file docblock (all slices shipped) and `STEP_ORDER` comment.
- [x] testIDs: `new-goal-build-row-{id}`, `new-goal-build-evidence-chip-{id}`, `new-goal-add-step-button`, `new-goal-build-ready-button` (reuses the pattern of existing testIDs like `new-goal-evidence-chip`).

### Step 2: Stories — BuildStep, InteractiveFlow, AllThemesMatrix

**Files**: `NewGoalWizard.stories.tsx`
**Commit**: `test(new-goal-wizard): build-step story, 4-step flow, AllThemesMatrix`
**Changes**:

- [x] `InteractiveBuildStep` wrapper (owns `buildSteps` + `openBuildStepEvidenceId` via `useState`), seeded with the prototype's own sample data (`initNG()`: "Sand the edges"/Note, "Paint it"/Photo) so the story matches the canonical mock exactly; export `BuildStep: Story`.
- [x] `InteractiveFlow` wrapper: owns `currentStep` (starts `"name"`), `goalTitle`, `firstStepTitle`, `plannedEvidenceType`, `evidencePickerOpen`, `buildSteps`, `openBuildStepEvidenceId`. Wires `onNext` to advance per `STEP_ORDER` (seeding `buildSteps[0]` from `firstStepTitle`/`plannedEvidenceType` on the `"step"→"build"` transition, D2), `onQuickAdd` to jump straight to `"build"` (matches the prototype's `ng.quickAdd`), `onBack` to step back one position, `onStartWorking`/`onClose` as no-ops (out of scope — no navigation). Export `InteractiveFlow: Story`.
- [x] `AllThemesMatrix: Story` (D6): static render of the wizard on `"build"` (the richest step — header, rows, chips, CTA all visible at once) with `buildSteps` seeded from the same sample data as `BuildStep`, wrapped in the toolbar-switcher explanatory note (mirrors `EditGoalView.stories.tsx`'s `AllThemesMatrix` text almost verbatim, substituting the actual re-render cause: "`NewGoalWizard` composes `EvidenceTypePicker`, whose animation-pref probe resolves async and re-renders after mount"). Import `themeNames` from `../../themes/compose` (only used for the toolbar note's theme-name list if enumerated; if the note is prose-only like `EditGoalView`'s, `themeNames`/`MOOD_NAMES` aren't imported at all — confirm against the simplest `EditGoalView` shape before adding unused imports).

### Step 3: Tests — build-step coverage + placeholder regression update

**Files**: `__tests__/NewGoalWizard.test.tsx`
**Commit**: `test(new-goal-wizard): step-3 build-list coverage + placeholder regression update`
**Changes**:

- [x] New `describe("build step")`: renders header (`"Your steps"` + count matching `buildSteps.length`); renders each row's title + evidence icon/label; pressing "+ add another step" fires `onAddStep`; pressing a row's evidence chip fires `onOpenBuildStepEvidence(id)`; sheet shows when `openBuildStepEvidenceId` matches a row, with that row's current type pre-selected; selecting a type fires `onBuildStepEvidenceTypeChange(id, type)` and `onCloseBuildStepEvidence()`; pressing the "I'm ready →" CTA fires `onNext`.
- [x] Replace `it("renders the build placeholder without a body regression", ...)` (currently asserts _no_ build content — now false) with real-content assertions for the "build" step, keeping the existing cross-step assertions (header label, back arrow, close button) it already covers.
- [x] Extend `it.each(["name", "step", "ready"])("fires onClose from the %s step", ...)` → add `"build"`.
- [x] Extend the back-arrow present/fires `it.each(["step", "ready"])` → add `"build"`.
- [x] Confirm the existing progress-segment (`4` rows incl. `"build"`) and `accessibilityValue` `it.each` tables need no changes (already parameterized over all 4 `NewGoalWizardStep` values).

## Testing Strategy

- [x] Unit tests for `NewGoalWizard` build step (Jest 30, `@testing-library/react-native` v13) — `bun run test --testPathPatterns NewGoalWizard` (58 pass).
- [x] `test.each` for the disabled/empty-state cases if any are introduced (none introduced — build's CTA is always enabled per D7; extended the cross-step `onClose`/back-arrow `it.each` tables to include `"build"`).
- [ ] Manual/Storybook: `Iteration B/Goals/NewGoalWizard` → `BuildStep` (add a row, open/close each row's evidence sheet, confirm independent chips), `InteractiveFlow` (click through all 4 steps, confirm data carries forward), `AllThemesMatrix` (switch the theme toolbar through all 7 product themes, confirm zero hardcoded hex and legible contrast in `highContrast`/`lowVision`). **← PENDING: not yet run; do before PR.**
- [x] `bun run type-check` and `bun run lint` clean (only pre-existing file-size warning on `NewGoalWizard.tsx`, 596 lines — expected, all 4 steps in one file per slices 1–3); zero hardcoded hex in the diff (`theme.colors.*`/`theme.radius.*`/`theme.space.*`/`shadowStyle` only). Full suite 9782 pass; `bun run build` no-op.

## Not in Scope

| Item                                                                                                    | Reason                                                                                                                                                                                                                      | Follow-up                                     |
| ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| Second-level nesting / smaller-steps tier on the build screen                                           | Explicitly deferred at the umbrella level (#443); `schema.ts` caps `parentStepId` at one level anyway                                                                                                                       | #465                                          |
| Build-row rename / reorder / delete affordances                                                         | Not present in the canonical prototype's `isBuild` route (D3/D4); out of this slice's literal scope. **Decided (Joe, 2026-07-06): mirror the prototype here (static rows); inline rename + delete tracked as a follow-up.** | **#482** — sub-issue of #384, blocked_by #464 |
| Screen/navigation/persistence wiring (real `t()`, Evolu writes)                                         | `NewGoalWizard` stays pure/prop-driven per D5 (#463); this is `[Integrate]`'s job                                                                                                                                           | #444                                          |
| Unifying step-2's `firstStepTitle`/`plannedEvidenceType` with step-3's `buildSteps` into one data model | D2 — bridging that seam for real (not just in a story) is `[Integrate]`'s job against actual Evolu data                                                                                                                     | #444                                          |
| `dep:blocked` label removal on #464                                                                     | Labeling is GitHub-metadata housekeeping, not a code change                                                                                                                                                                 | none — flag at finalize/PR time               |

**Sizing note**: the estimate above (~460-560 lines) sits at or slightly past the issue's own ~400-500 LOC estimate and this repo's general ~500-line PR guidance, because this slice also has to touch the two already-shipped steps' shared plumbing (the `EvidenceTypePicker` derivation logic, D1) and add three stories, not just one. It is not being split further because (a) the issue explicitly scopes "step 3 + flow story + AllThemesMatrix" as one slice of the pre-agreed 3-way split (#443), and (b) the flow story and matrix both structurally require the build step to exist first, so splitting them into a 4th slice would just reintroduce the "blocked by" chain #443 was designed to avoid. If review flags the diff as too large in practice, the safest split point is: commit 1 (component+styles) as its own PR, commits 2-3 (stories+tests) as a fast-follow — both still gated on #464 being "done" per #443's own closing condition ("Done when #462, #463, #464 are closed").

## Discovery Log

<!-- Entries added by implement skill:
- [YYYY-MM-DD HH:MM] <discovery description>
-->

- [2026-07-06] Build-row evidence chip renders **icon + label** (e.g. "📝 Note"), not the prototype's icon-only pill (`App Shell.dc.html:379` shows `{{ step.evIcon }}` alone). Followed the plan/issue intent (Intent-Verification line: "purple evidence chip with icon+label"; Step-1 checklist: "same icon/label styling as the step-2 chip") over the prototype — a deliberate, documented richer chip consistent with step 2, reusing the shared `evidenceChip` style. Flagged here since it diverges from the canonical mock.
- [2026-07-06] Trailing step-body branch is `null` (unreachable — `currentStep` is always one of the 4 STEP_ORDER values) rather than a rendered placeholder View; the `placeholderBody` style is removed. Added `evidenceIcon()` helper (shared by the planned chip and build rows) to avoid duplicating the `EVIDENCE_OPTIONS.find(...)` lookup.
- [2026-07-07] Build-header count needed a dedicated `testID="new-goal-build-count"` — the count value collides with a row-number value (e.g. count `2` vs. row-2's `2`), so `getByText("2")` was ambiguous. testID disambiguates the D8 assertion. Story wrapper `InteractiveFlow` function renamed to `InteractiveFlowWizard` (collided with the `InteractiveFlow` story export); `BuildStep` type imported into stories as `BuildStepData` (collided with the `BuildStep` story export).

## Implementation Status (2026-07-07) — for context clear / handoff

**Branch:** `feat/issue-464-newgoal-build-list` · **3 commits, all DCO-signed, none pushed yet.**

| #   | SHA        | Commit                                                                                                      |
| --- | ---------- | ----------------------------------------------------------------------------------------------------------- |
| 1   | `2b7b226e` | `feat(new-goal-wizard): step 3 build list — rows, add-step, evidence chip` (NewGoalWizard.tsx + .styles.ts) |
| 2   | `fa02a6b7` | `test(new-goal-wizard): build-step story, 4-step flow, AllThemesMatrix` (NewGoalWizard.stories.tsx)         |
| 3   | `b7f764c3` | `test(new-goal-wizard): step-3 build-list coverage + placeholder regression update` (test + count testID)   |

**Green:** type-check ✅ · lint ✅ (only pre-existing 596-line file-size _warning_) · tests ✅ (58 NewGoalWizard, 9782 full suite) · build ✅ (no-op).

**Remaining before PR:**

1. **Manual Storybook visual pass** (the one unchecked box) — `BuildStep` (add row, open/close each row's sheet, independent chips), `InteractiveFlow` (click all 4 steps, data carries forward), `AllThemesMatrix` (toolbar through all 7 themes; check `highContrast`/`lowVision` contrast). **Watch the icon+label vs. icon-only chip divergence here** — confirm the richer chip reads well against the prototype.
2. **`/finalize`** — push branch, open PR. At PR time: flag `dep:blocked` label removal on #464 (stale, per Dependencies §) and note umbrella #443 closes when #462–#464 all close.
3. Follow-up **#482** (inline rename + delete on build rows) stays out of scope (Not in Scope §).
